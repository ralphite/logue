# typescript-eslint 8 still declares a peer range that stops at TypeScript 6,
# and TypeScript 7 is what the build actually uses. The parser handles the same
# syntax, so accept the resolution here rather than hold the toolchain back —
# `npm run lint` in CI is the check that this stays true.
legacy-peer-deps=true
