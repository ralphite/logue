# Logue 项目目标与完成契约

状态：**进行中，未达到完成标准。**

本文件是 Logue 的最高级项目约束。实现、设计、测试、评审和自动化都必须以它为准；不能用“构建通过、测试通过、写了文档、做了截图、Storybook 有状态”代替真实用户结果。

## 1. 最终目标

把 Logue 做成一个真正好用、完成度高的本机优先工具：用户不离开当前网页，就能准确完成语音/文字输入、采集选区并添加批注；所有主动保存的内容永久沉淀为可追溯资料，可被项目记忆、可编辑文档和外部 Agent 继续复用。

产品要达到以下整体质量：

- 默认核心流程与 ChatGPT 原生语音一样直接，不能因为“更完整”而更繁琐；
- Web App 的信息架构、内容密度和编辑体验达到 Notion 式的安静、清晰、内容优先；
- 桌面/平板左侧栏可像 ChatGPT.com 一样随时折叠成安静图标栏，状态稳定且不影响移动端任务导航；
- 不是 Notion 的仿制品：跨网页输入、原始音频、来源链、项目记忆、受控 Agent 写回是必须成立的差异化能力；
- 桌面端、手机端、Chrome Extension 都是完整产品表面，不是演示页面；
- UI/UX 具体方案由产品判断决定，但最终判断只有一个：真实任务是否非常好用。

## 2. 不可妥协的核心闭环

### 2.1 当前网页输入

`聚焦真实网页输入目标 → 麦克风入口自动出现 → 点击开始录音 → 停止并插入 / 取消`

- 入口只在某一个真实输入框当前聚焦时出现；失焦、卸载或路由切换后立即消失。
- 点击麦克风立即录音；录音态只有“停止并插入”和“取消”两个用户决策。
- “停止并插入”自动完成转写、保存和插入；不进入转写审阅，不要求重录、项目、Tag、参考或归档选择，也不存在第二次确认。
- “取消”立即结束并丢弃本次未采用录音，不产生资料或插入。
- 必须提供直接开始/结束这条链路的快捷键。
- 项目识别、Tag、纠错和整理都是后台自动或事后操作，不得增加语音输入的前置步骤。
- 新资料保存后，后台必须自动归并到合理项目并生成 Tag；高置信度结果安静完成，只有存在疑问或低置信度时才高亮请用户复核。
- 必须先把最终文字、来源、转写、音频和项目关系保存成功，再写入宿主网页。
- 永不按 Enter、永不点击发送、永不自动提交网页表单。
- 保存或插入失败必须可恢复；重试不能重复保存或重复插入。
- 目标输入框消失时，已保存资料不能丢失，并提供重新插入或复制。

### 2.2 网页选区与批注

`选中文字 → 右键“保存到 Logue” → 查看完整原文 → 可选文字/语音批注 → 保存`

- 原文不可被批注或 Agent 结果覆盖。
- 原文与批注是独立资料，使用父子关系追溯。
- 可以不选项目直接保存，也可以关联一个或多个项目和 Tag。
- 已保存资料必须能继续追加批注或执行指令。

### 2.3 资料、项目与生成

- 一级对象使用用户可理解的 `资料流 / 项目 / 生成 / 设置`；`View`、`Context`、“成果”等技术或含混名称不得出现在产品 UI。
- 资料流保存 voice、selection、text、derived 等全部主动内容；默认永久保留，只有用户明确删除才移除。每条资料的内容、项目与 Tag 都始终可修改。
- 项目不是普通标签，必须包含可编辑背景、确认术语、关联资料和生成结果；资料可属于多个项目。
- 系统默认自动归项目与 Tag；不确定项在资料流和详情中有明确但不制造待办压力的“需要确认”标记，用户修改后即视为确认。
- “生成”不等于文档：结果可以是可直接插入的短回复、消息、QA、派生资料或可持续编辑的文档。
- 文档作为一种生成结果，采用 Notion 式列表与编辑体验：可新建、生成、编辑、自动保存、继续使用。
- 生成内容必须保留稳定行内引用；来源面板与正文编号一一对应，点击可定位，高亮且可增删。
- 任何派生内容都保留创建者、具体来源版本和父子关系，不覆盖原始资料。

### 2.4 项目记忆、Gemini 与 Agent

- Gemini API Key 只从本机 Go 进程的终端环境变量读取，不能进入浏览器、Extension 存储、数据文件或日志。
- 转写模型、技能和 Context 上限可配置，真实中英文、长句和项目术语质量至少与同工作区的 `../prototypes/vibedoc` 对齐。
- 默认最多使用一个参考项目；不能因为多项目归属而自动混入多个项目 Context。
- Agent 是可持久、可编辑的用户对象，不是写死在代码里的一个 Prompt。用户可创建多个 Agent，至少定制名称、目的、指令、可用上下文、输出形态和可出现的产品表面。
- 转写、资料归项目/加 Tag、基于资料生成回复/消息/QA/文档，都由可替换或可定制的 Agent 执行；系统可提供高质量默认 Agent，但不锁死用户。
- Agent 可在 Web 生成页和 Extension 的当前输入目标中使用。Extension 中“生成回复”是麦克风旁的独立能力，不得增加极简语音输入的步骤。
- Logue Web App 自己的编辑器与输入框也必须能够使用 Logue Extension；只能通过显式页面/子树 opt-out 避免递归，不能把整个 Logue Web App 默认排除。
- 只有用户最终采用、明确固定或可靠反复出现的表达能形成强记忆，避免错误自我强化。
- 外部 Agent 通过只读项目包读取内容；写回只能追加带来源、带 actor、可幂等的派生资料或文档。
- Agent 不得静默改写原文或已确认项目知识；项目概览更新必须由用户显式采用。

### 2.5 GitHub 发布与一行安装

- `https://github.com/ralphite/logue` 的既有实现已经完全废弃；远端旧代码、旧分支历史和旧发布不再作为产品来源，必须由当前 Logue 完整替换。
- 当前仓库必须成为新的唯一代码来源，但真实 `.logue-data`、音频、Gemini Key、浏览器状态、临时 QA 资料和其他本机隐私数据绝不能进入 GitHub 或 Release。
- GitHub Release 必须提供稳定的一行安装入口：用户通过 `curl` Release URL 并交给 shell 即可完成安装，不要求手动 clone、构建或复制文件。
- 安装器必须同时支持全新安装与覆盖升级：识别并停止已安装的旧 Logue，原子替换程序与 Web/Extension 资源；任何已有用户数据目录都不得删除、清空、迁移失败后覆盖或回退成示例数据。
- 安装结束必须自动启动服务并等待健康检查通过，再显示可访问地址和下一步；失败时给出可恢复错误，不留下半安装版本。
- 安装过程必须明确询问是否加入本机开机自动启动，默认选择应清晰；无交互环境允许通过环境变量显式选择，不得卡死。
- 自动启动与普通启动都不能把 Gemini Key 写入仓库、Release、浏览器、Extension 存储、数据文件或日志；服务从本机进程环境读取 Key。
- Release 资产必须有版本、校验和与支持平台说明；至少真实验证当前 macOS 架构的全新安装、重复覆盖安装、数据保持、停止旧服务、立即启动和开机启动配置。

## 3. 产品与视觉质量要求

- 设计参考 Notion 的信息架构、编辑体验和低干扰视觉原则，而不是机械复制外观。
- 对照 ChatGPT 原生语音流程，保证默认输入路径没有多余确认和无价值选择。
- 删除头像、在线状态、泛统计、泛 Agent 建议、装饰卡片、红点式整理压力等低价值 UI。
- 任何可见模块都必须回答一个明确问题或完成一个高价值动作。
- 极简是强制设计原则：每一个可见元素都必须能解释它对当前任务的价值。正常、成功、已连接、运行中等无需行动的状态默认不占用界面；只有异常或需要用户行动时才显示。按钮文案使用能表达动作的最短词，例如可判断上下文时只显示 `Copy`，不得重复用户已经看得见的对象名。
- 同一组件或同级选项必须共享完全一致的布局骨架、尺寸、圆角、选中面和交互状态；selected、hover、focus 只能改变预先定义的状态属性，不能因实现分支导致背景范围、对齐或命中区变化。所有前端改动都必须以资深产品设计审查标准检查视觉层级、节奏和状态一致性。
- 使用明确且可复用的内容列体系，禁止页面各自随意定义宽度：文档、项目详情、设置、生成表单/结果与 Agent 编辑器共享 820px 阅读/编辑列、相同水平 padding 且 header 与正文共轴；资料流等高密度列表才使用单独的 1080px 宽列。任何例外都必须由任务密度而非实现便利性解释。
- 真实内容优先，避免示例感、Dashboard 感、技术后台感和同质化卡片堆叠。
- 所有可见状态必须完整：默认、hover、focus、pressed、disabled、loading、empty、success、error、offline、retry、overflow 和窄屏。
- 手机端必须是经过重新编排的任务体验，不能只是把桌面三栏压窄；核心导航、编辑、来源和操作都要可用。
- 桌面端各竖向分栏边界必须使用同一套可拖拽 resizer；命中区易用但视觉低干扰，宽度约束稳定、可复位、可键盘操作并记忆用户设置。移动端继续使用为窄屏重排的抽屉/单栏交互。
- 右侧面板的最大宽度必须动态使用工作区全部剩余空间，不能被固定像素上限截断；窄屏右侧面板直接占满可用宽度。
- 主侧栏展开/折叠只改变容器宽度与文字可见性；一级导航图标的绝对水平位置不得移动，交互细节以 ChatGPT.com 的真实展开/折叠行为为质量参照。
- 主侧栏不得另设右侧折叠按钮。鼠标进入侧栏任意位置时，左上角 Logue 品牌图标在原位切换为当前状态对应的展开/收起图标；移出后恢复品牌图标，展开和折叠两种状态都保持同一水平锚点，并保留完整键盘与无障碍操作。
- “生成”侧栏只保留 `Documents` 与 `Agents` 两个入口；点击入口只切换下方对应列表。每一行最右侧提供独立新建按钮：Documents 新建进入生成文档流程，Agents 新建可编辑 Agent。不得保留重复的顶层 `New` 或通用加号，切换两类对象时保持同一页面骨架。
- 产品名词必须面向用户；技术实现名不能直接成为功能名。
- Web App 的全部用户界面使用英文；Web App 实现代码中的标识符、注释、无障碍文案和测试描述也使用英文。

## 4. 技术与运行要求

- Web App：React + TypeScript + Tailwind CSS。
- 组件与状态：Storybook。
- 本机 API 与数据层：Go。
- Chrome Extension：真实安装、真实网页输入目标、真实选区右键和真实断线恢复。
- 数据默认保存在本机，并在刷新、服务重启、浏览器重启后保持。
- 必须支持可验证的整库导出与恢复；恢复前创建完整备份。
- 手机必须可通过同一局域网访问 Web App；不能因 API 写死 `127.0.0.1` 而只显示空壳。
- 开发交付时保持可访问服务运行，并明确桌面与手机入口。
- 远端唯一发布仓库为 `ralphite/logue`；发布必须由可复现构建生成带校验和的 Release 资产，安装器不得依赖本地源码树。

## 5. 真实验收场景

以下场景必须使用真实 Chrome、真实 Web App、真实 Go 服务和真实持久数据通过：

1. 在标准 textarea/input 聚焦后看到唯一麦克风入口，一次点击开始录音，再点“停止并插入”后自动转写、保存并插入；网页出现文字且资料、音频和转写可追溯。
2. 在明确支持的富文本编辑器完成同样流程，且不自动提交。
3. 选中网页文字，右键保存，并分别验证无批注、文字批注、语音批注。
4. 在多个项目间使用不同术语和背景，证明参考项目生效且不会跨项目污染。
5. 创建多条不同来源的资料，验证可定制整理 Agent 自动归项目与 Tag、低置信度复核、内容可编辑；再用不同 Agent 生成短回复、QA 和文档，文档编辑、自动保存、引用、来源定位和增删全部正确。
6. 创建或复制一个 Agent，修改其指令与输出形态；在 Web 生成页使用它，结果与实际采用来源可追溯。
7. 在真实 Web 聊天输入框使用 Extension 的生成能力，基于相关资料生成一条回复并插入；不自动发送。
8. 外部 Agent 读取项目包并幂等写回带来源结果；原始内容保持不变。
9. 断开 Go 服务后看到可恢复状态；恢复后重试成功且没有重复资料或重复插入。
10. 刷新和重启后，资料、音频、项目、Agent、生成记录、文档、来源关系、设置仍存在。
11. 用真实音频比较 Vibedoc：中文长句、英文、中英混合和项目专有词均达到质量门槛。
12. 在真实手机宽度和手机浏览器访问完整 Web App，完成资料浏览、项目浏览、生成与文档编辑和来源查看。
13. 从一台无现有安装的隔离环境执行 Release 的一行 `curl` 安装命令；无需源码或构建工具，安装后服务自动启动、健康检查通过，Web 与 Extension 资产完整可用。
14. 在已有 Logue 与持久数据的隔离环境再次执行同一命令；旧服务被安全停止，程序被覆盖升级，资料/音频/项目/Agent/文档/设置保持不变；分别验证选择与拒绝开机自动启动，过程不泄露 Gemini Key。

## 6. 完成判定

只有同时满足以下条件，才允许把目标标记为完成：

- 所有用户原始要求已进入可追踪需求文档，且不存在被模型自行生成的错误约束；
- 上述核心闭环与 14 个真实场景全部通过，不是 Mock 或只在测试环境通过；
- 当前真实数据无明显错误、陈旧内容、重复对象或错误关联；
- Notion/竞品对比和 ChatGPT.com 截图盲审没有未解决的 P0/P1 结构问题；
- fresh-context 独立代理重新阅读全部要求、检查真实产品并给出完成结论；
- 继续修改的预期用户价值已经明显低于风险与成本，即边际收益有限。

以下证据**不能单独证明完成**：文档、计划、截图、Storybook、单元测试、构建、提交、代码量、工具调用次数、Agent 数量或“看起来不错”的主观表述。

## 7. 持续审查机制

- 当前原生 `/goal` 是执行载体；未达到完成判定前不得自动暂停或提前结束。
- `Logue 每小时目标审查` 心跳每小时唤醒当前任务。
- 每次审查先启动 fresh-context、只读 `goal_supervisor`，逐项对照本文件和 `docs/user-requirements.md`，给出 `CONTINUE` 或 `REPLAN`。
- 审查后自动继续唯一最高价值的实现与真实验证；不得把审查本身当成产品进展。
- 任一后续用户纠正与本文件冲突时，先更新本文件和需求文档，再继续实现。
- 维护逐项状态 tracker，覆盖全部历史 bug、feature request 与审查发现，明确 `done / partial / todo / blocked`、真实证据、缺口与最短验收；独立审查必须能重新发现被遗漏或只实现未验证的项目。

## 8. 需求事实来源顺序

1. 用户最新的明确纠正；
2. 当前 Codex 任务内的用户要求；
3. 原始 ChatGPT 会话：`https://chatgpt.com/g/g-p-6a6e3ab1be3c81918740f240d4d0b21a/c/6a6e3b36-42f8-83e8-a8cb-1e792754f2da`；
4. `docs/user-requirements.md` 中已经核实的忠实记录；
5. 产品规格、交互规格和设计判断。

任何模型生成的流程、名词或 UI 方案都低于用户明确要求，不能反向改写需求。
