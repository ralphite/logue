{
  "$schema": "./node_modules/oxlint/configuration_schema.json",
  // The real-browser checks in scripts/qa are sequential drivers: printing is
  // their output, and awaiting inside a loop is what "do this, then look" is.
  // Linting them by the application's rules only teaches people to silence it.
  "ignorePatterns": ["**/dist/**", "**/storybook-static/**", "scripts/qa/**"],
  "plugins": ["typescript", "react", "unicorn", "oxc"],
  "categories": {
    "correctness": "error",
    "suspicious": "error",
    "perf": "error"
  },
  "rules": {
    "no-console": ["error", { "allow": ["error", "warn"] }],
    "eqeqeq": ["error", "smart"],
    "typescript/consistent-type-imports": "error",
    "typescript/no-explicit-any": "error",
    "react/rules-of-hooks": "error",
    "react/exhaustive-deps": "error",

    // The build uses the automatic JSX runtime; importing React is dead weight.
    "react/react-in-jsx-scope": "off",
    // Menu takes its trigger as a render prop so the caller owns the button.
    "react/no-unstable-nested-components": ["error", { "allowAsProps": true }],
    // An effect that cleans up conditionally is the normal React shape.
    "typescript/consistent-return": "off"
  }
}
