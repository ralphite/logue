# 任务队列

要做的事,和它们现在的状态。**这个文件是唯一的正本** —— 会话里的任务列表只是它的一份镜像,会话结束就没了。只存在于会话里的东西,一次崩溃就丢,而这份清单已经丢过一次。

**每一条新要求都在提出的当下写进这里**,写完再动手。只出现在回复里的要求不算入队。

两个文件,两件事。这里管**顺序**和**还没做的**;[behaviors.md](behaviors.md) 管**做完之后必须成立什么**,写成能扛过下一次重写的样子。一条要求通常两边都要落。

还有第三个:[proposals.md](proposals.md) —— 你要求"先出方案不要实现"的那几件,方案写在那里,每节末尾是要你回答的问题。
第四个:[audit.md](audit.md) —— T1 全面走查的结果,六条值得修的,按值不值得排好,等你点哪几条要动。
另外两份单独的方案各自成文:[panel-ia.md](panel-ia.md)(N1b,面板信息架构 A/B)和 [dictation.md](dictation.md)(N4,面板里的 Dictation)。

报 bug 的截图存进 `docs/spec/shots/`,被 git 忽略,由任务按文件名引用。图跟着任务走,不进仓库。

## 现在,按这个顺序

**活跃队列现在在 Context Center**([../../cc/tasks.json](../../cc/tasks.json),浏览器开 `http://127.0.0.1:8788/`)。那边是**日常进出的地方** —— 每一条新要求连同你的原话一起落在那里,靠 `/cc` 写入。这份文件仍然是**正本**:队列的快照抄在下面,做完的沉进"已完成",长期规则和行为约定只在这里和 [behaviors.md](behaviors.md) 里。**两边不一致时,以 Context Center 为准**,并把这里同步过来。

下面这份快照同步于 **2026-08-12**。

| | 任务 | 状态 / 为什么在这 |
|---|---|---|
| **N12** | 左栏的 + 移进内容区,列表页头样式修掉 | `done` · P1 · 2026-08-13。`+` 从导航条(悬停才出现)移到 Projects / Documents / Skills 各自列表的页头右上;列表页头首行从 42px 改成 48px,和右侧详情页头对齐(实测原来是 42 对 48)。 |
| **X3** | Settings 的 Model 框:当前模型只当 placeholder 显示 | `done` · P2 · 2026-08-13。已保存的模型名现在填进 value(表单加载时填,且不会覆盖正在打的字),placeholder 只留默认值提示。API key 仍然不显示。 |
| **X2** | 点开文档不应该改动它的更新时间 | `done` · P2 · 2026-08-13。两处原因:编辑器把 Host 传来的正文当成按键(现在带标记不算编辑),以及内容没变的保存仍写 `updated_at`(现在完全不写)。 |
| **T1** | 面板的四条浏览器检查按新面板重写 | `queued` · P2 · 未确认 · audit-ext / cuj-panel / a1 / x38 仍照旧面板四个 tab 写,现在必红。**故意不重写**:等你对 N13(面板收成一个列表 + 一个 composer)拍板,否则白写两遍。scripts/qa/README.md 已注明。 |
| **F1** | 转写撞上模型过载时自己重试,别让人去点 Try again | `done` · P1 · 2026-08-13 你的原话 "add task to tasks.json: auto retry when this err",附一张 503 的截图。Host 在一次请求里重试四次(1s/2s/4s,认 `Retry-After`,上限 20s),面板再自己试两次(5s/15s),期间行里写 `The model was busy. Trying again…` 并能放录音;都失败才出错误和 Try again。**只重试会过去的失败**(408/425/429/500/502/503/504、连不上),400/401/403 一次就报。声明见 [features/busy-model.md](features/busy-model.md),真浏览器检查 `scripts/qa/f1.mjs`。 |
| **F2** | Document 大改:第一行就是标题,Markdown 要所见即所得(参考 Vibedoc) | `done` · P1 · 2026-08-13 你的原话:文档不该有独立的 title 段,像 Google Docs 一样第一行就是标题;要真的所见即所得地编辑 Markdown,参考 `~/dev2/prototypes/vibedoc` 的设计和功能。已落地:标题输入框删掉,名字=第一行(去掉 Markdown 记号,50 字截断);编辑器换成 CodeMirror + Markdown,标题就是大字、粗体就是粗体,**光标不在的那一行记号自动隐藏**;存的就是 Markdown(不再存 HTML),导出/diff/模型读的是同一份文本;旧工作区开机时转换一次,原来的名字写成第一行标题,一条都不丢。**顺带砍掉**:模型给文档起名那一步(名字现在就在正文里)。声明见 [features/documents.md](features/documents.md)。 |
| **X1** | 扩展 sidepanel/widget 和 Web 应用的数据要保持同步 | `done` · P1 · 2026-08-13 落地:Host 记一份「写了多少次」的计数(按种类分),`GET /v1/changes` 不读任何文件;面板和 Web 应用各自每 1.5 秒问一次,数字变了就**静默重载**(不转圈、不弹条、不加按钮)。**正在打字没保存的地方不重载**。看不见的面板不问。真浏览器检查 `scripts/qa/x1.mjs`:面板开着,从别处建一个 Project,不点任何东西它就出现了。声明见 [features/in-step.md](features/in-step.md)。 |
| **N10** | Web 应用整页 IA 重构:三个方向的 preview 等他选 | `in_progress` · P0 · redesign-c 分支上 C2 已落地,等你选定方向。 |
| **N13** | Extension 面板收成「一个列表 + 一个输入框」 | `blocked` · P1 · **等你回答三个问题**,在 [panel-composer.md](panel-composer.md) 第六节。三个动词收成一个输入框;选中页面文字自动以 Markdown 引用进框;录音条三键(✕ 丢弃 / ✓ 转文字插入 / ↑ 插入并提交),**插入不等于提交**,允许多次口述;提交出来的每条记录底下挂 Ask · As Markdown · Into English。**你点头前一行产品代码都不动。** |
| **N9b** | redesign 第二轮:Markdown 渲染、Notice/EmptyState 统一、浮动条不压字段 | `queued` · P1 |
| **N11** | Extension sidepanel + 注入 widgets 按 C2 语言重设计 | `queued` · P1 |
| **N1b** | 按 [panel-ia.md](panel-ia.md) 重排面板 | `blocked` · P1 · **等你在提案第六节回答三个问题**。提案已对着今天的面板刷新过(8/10 写的时候三个 tab,现在四个、默认开 Dictation):原第 5 条 N5 已修掉,新增 2b(两个 tab 抢同一个录音机)和 10(默认屏由排期决定,不由入口决定)。A:四个 tab 收成两个,Chat 和 This page 合成一条流。B:四个留着只按主语重排。**我推荐 A。你点头之前一行代码都不动。** |
| **S3g** | 在登录着的 Google Docs 上跑 cuj-gdocs | `blocked` · P1 · **要你浏览器里的登录态**。2026-08-11 你已经点头:用你自己的 Chrome、新建一个空白文档来跑。但你紧接着说"focus on items not blocked",所以**我没有动你的浏览器** —— 这一条随时可以开跑,说一声就行。 |

**两条卡在你身上**:S3g(你的登录态,已点头待开跑)、N1b(等你 confirm 提案)。其余按顺序推进,X1 是新报的 bug,排最前。

## 长期规则(按你说的原话记)

这些是工作约定,不是任务。每一条在 [behaviors.md](behaviors.md) 里都有对应的详细版本。

- **自己管队列、自己往下推。** 顺序你来定,不要等人喊继续。
- **上一个做完立刻做下一个。** 有值得读的东西才汇报,不要每做一件报一次。
- **能量出来的,不要拿来问。** 你的原话是 "this is low quality qustion. avoid in future. why cant you measure? what do you think it could be?"。只有答案长在你脑子里的事才值得问你 —— 你想要什么、你偏向哪个取舍、什么程度算够好。**跑一下、读一下代码、掐个表就能知道的,不是问题,是我该去做的事。** 把数字带来,说清它对决定意味着什么,并给出建议。实在还量不到的,就说出预计是多少、依据是什么 —— 至少要有个能被证伪的判断,而不是把空白丢回给你。
- **不要停,做没被卡住的那件。** 你的原话是 "work on things not blocked. avoid being blocked."。有东西挡路就绕。真需要你拍板的那件,把问题写清楚留在队列里,然后**立刻往下做别的** —— 队列里永远有不需要等任何人的事。也要主动少给自己制造卡点:先做不依赖别人的部分,把要问的问题攒着一次问完,而不是问一句停一次。
- **短暂浮层上不要多余的字。** 你的原话是 "why the inserted and undo text? we should remove all of these useless words in UI"。人人认识的动作(撤销、关闭、勾)用图标 + 悬停提示,不配文字说明。**与"图标必须带字"不冲突**:那条禁的是没人看得懂的状态点(蓝点),这条删的是给已经看得懂的东西再配的字幕。
- **界面必须一致 —— 每一处、每一层。这是地基,不是收尾。** 你的原话是 "ui design must be consistent! everywhere! all levels! this is the basics."。同一件事在哪儿都得长得一样、用起来一样:页面宽度和边距、分区标题和空态、行/卡片/列表、按钮/输入框/菜单、图标、间距、字号、颜色、文案。两个屏幕用两种方式解决同一个问题,说明其中一个是错的 —— 挑一个用到两处,不是两个都留着。**在这之上,还要照 UI 设计的正经标准做审计**:层级、对齐、对比度、点击区域、焦点与键盘顺序,以及加载/空/出错/内容过多这四种状态,还有文案有没有说清会发生什么。这些要**成系统地查一遍**,不是碰巧看见才修。
- **拿不准就先问我,别自己猜。** 你的原话是 "confirm with me in case of something you are not sure about"。一句话有两种读法、两种读法会导致两种做法时,**停下来问一句**,不要挑一个顺手的往下走 —— 猜错了要么白做,要么把已经对的东西改坏。这跟"能量出来的不要拿来问"不冲突:能跑一下、读代码、掐表得到的,自己去拿;**只有你脑子里才有的答案(你要什么、你偏向哪个),就问你**。
- **拿不准一个功能该长什么样,就不要动手。** 你的原话是 "if you are not sure how the feature should be like do not work on it. instead propose and let me confirm first"。不确定的时候写方案、等确认,**不要先做出来再问**。做出来的东西会替你把决定做掉一半 —— 它已经存在了,推翻它比选它贵。
- **新的要求进队列,不插到手上这件前面** —— 除非它现在就是坏的。
- **队列在 Context Center,正本仍是这份文件。** 你的原话是"我们同步一下 tasks.md 和这个 JSON 文件,然后 JSON 文件里的 item 我们都用中文"。日常进出走 `/cc`(写进 [cc/tasks.json](../../cc/tasks.json),浏览器 `http://127.0.0.1:8788/` 看);**JSON 里的标题、说明、卡住的原因一律中文**,只有 `--said` 那一栏是你的原话、一个字都不许改。这份文件保存队列快照、已完成、长期规则;**两边不一致以 Context Center 为准**,再同步回来。
- **每一条被要求的行为,在被提出的当下就写下来**,让重写不能悄悄把它弄丢。
- **竞品的功能清单是菜单,不是命令。** 值得的留下,其余删掉。标准是极简、一眼就懂,不是"抄全"。
- **加回旧功能、采纳竞品功能,先列清单等你确认。** 你的原话是 "do not add back before i confirm"。
- **现有功能默认保留。** 你的原话是 "default to keep all unless very unnecessary"。
- **持续打磨不是一次性任务。** 你的原话是 "polish the ux/product design/features to make it very good. keep pushing automatically in this way"。
- **只在这个会话里干活。** 你的原话是 "only work in this session"。
- **每次加完任务,看一眼干活那个会话还在不在。** 你的原话是"你加上 task 的时候,同时看一下另外一个 session 状态……如果它还在 running 就没关系;如果它已经停下来了,你可以告诉它我们这边有新 task"。干活的那个会话叫 **"N4 work"**(2026-08-12 之前叫 "long session 1",那个已经停了,别叫它)。跑 `/peer` 就行 —— 查状态、停了就把新任务逐条告诉它、并让它先 `git pull` 再读 tasks.md。**没人读的队列只是一份文档。**(查状态只能用 `list_sessions` 这个 MCP 工具:CCD 的会话不在 `~/.claude/projects/` 的 jsonl 里,`~/.claude/sessions/` 也只记当前活着的进程 —— 两处都不认识它,所以做不成 shell 脚本。)
- **该提交就提交,能推就推。** 你的原话是 "must commit when necessary. must push when possible"。一件事做完、验过,就落成一个提交并推上去 —— 只在本地的东西,一次机器故障就没了。
- **提交前必须逐个文件看 diff,不认识的文件绝不进提交。** 你的原话是 "alway check the diff when commit. never files you do not know about. check each file/diff before commit"。455 个文件的泄露正是"没看就 add -A"的代价;机器上的闸(check-secrets)是兜底,读 diff 是本分,两个都要,谁也不替谁。
- **暂时没有模型 key:用替身模型顶着,活不能停。** 你的原话是 "i dont have a key now. you must mock and continue the work"。这是你对"不许 mock"规则的**明确豁免**,只豁免模型这一层:Settings 里 key 填 `mock` 即启用,status 里 model 明写 mock,每条产出自报身份。**mock 之下验过的每一件都要打标,真 key 一到全部重验**(见队列 S3)。
- **在真实浏览器里、用真实 Host 和真实模型验证。** 不拿 mock 顶替。
- **要登录才能测的,就用你自己的浏览器测。** 你的原话是 "must use my browser with auth in qa when necessary (e.g. use app that needs login)"。Notion、Google Docs 这类必须登录的地方,不许用一个干净的测试浏览器绕过去,也不许拿"需要登录"当作跳过验证的理由。用带登录态的那个浏览器,并且只做验证要做的事:读、往 QA Project 里写,不删任何东西、不往外发任何东西。
- **不许拿自制的测试页当验证。** 你的原话是 "do not use these to test. use real content."。就是截图里那种 "Draft / Existing text. / Article / Asynchronous research…" —— 自己写的假页面,字少、结构干净、什么都刚刚好,而真实网页从来不长这样。要用真实内容:真的文章、真的 Notion 页面、真的 Google Doc、真的聊天输入框。一个假页面上跑通,证明不了任何事。反例存在 `shots/rule-no-synthetic-test-page.png`。
- **优先在 Logue 自己的应用里测,而且要用内容复杂的长文档。** 你的原话是 "prefer testing in logue app with a lot of complex text doc."。你给的正例就是那种真实文档:中英混排的长文、多级标题和列表、满篇 `[Source n]` 高亮引用、选区工具条压在正文上。这种页面才会把换行、选区、光标定位、浮层遮挡这些问题逼出来 —— 三行字的干净页面永远逼不出来。
- **必须在 Notion 里测。** 你的原话是 "must test in notion"。凡是碰到浏览器界面的改动,Notion 和 Google Docs 都要过 —— 测试页上的一个普通输入框证明不了任何事,光标、选区、重绘在这两个编辑器里都不一样。
- **验证写进 "Logue QA" Project,并且不删任何东西。**
- **你的浏览器上必须是最新的扩展,并且要定期更新。** 你的原话是 "my browser must have latest extension. update it regularly."。不只是部署后那一分钟 —— 要定期检查并推上去,让"我现在看到的是哪个构建"永远只有一个答案:当前那个。**拷进安装目录 ≠ 浏览器里正在跑** —— 一个开了一整天的标签页照样可能还在跑上周的脚本(X19 就是这么来的)。
- **该提交就提交,能推就推。** 你的原话是 "must commit when necessary. must push when possible."。一件事做完、验过,就落一个 commit,别攒着 —— 攒成一大坨,出问题时没法一步步往回退。有远端就推上去;推不上去(没配远端、没网、被拒)就说一声,别默不作声地留在本地。
- **dev server 可以用,但节奏由"一件事做完"决定。** 你的原话是 "你可以用 dev server,但是要及时的 reload 它。你修完了一个 bug 或加了一个功能,你就要去直接 reload。这个频率比你改一行代码它就自动 reload 要好很多"。所以:**修完一个 bug、加完一个功能就立刻部署**,不攒着;你开着的页面最多 20 秒自己换到新构建(有未保存的字先问你,见 X27)。这条**限定了**下面"一台机器只有一套"那条 —— 它管的是**你用的那一套**必须唯一,不是禁止我在开发时另起一个。**但验证仍然只认你装的那个 Host 和那个构建**:在一个你不跑的地址上验过,不算验过。
- **一台机器只有一套** —— 一份代码、一个 Host、一个扩展 —— 而且全部装好、跑着,随时能查,不需要开终端。
- **产品里的一切是英文,对你说话是中文。** 你的原话是 "use english in product features and code"。界面文案、按钮、空态、错误信息、代码与注释、提交信息 —— 全部英文;我对你的解释、方案、清单 —— 中文。**两者不许混**:mock 里的界面也算界面,不能因为是给你看的就写中文。
- **用中文回复。**

## 已完成

**README,以及让它那条安装命令能成立(你说的"我希望有一个 Readme 文件……需要有一个 cURL 的命令可以让他安装")** —— README 写在仓库根目录:装什么、怎么用、东西放在哪、怎么升级卸载、怎么开发和发布。<br><br>写的过程中撞到两件让那条命令不成立的事,都修了。**Host 去 `<install root>/web` 找应用,安装器却把它放在 `current/web`** —— 于是用 curl 装完打开首页只会返回 JSON,README 上唯一那个地址给的不是应用。修法是一条穿过 `current` 的固定软链,升级失败回滚时应用跟着一起回去。**自检一直没发现,是因为它只读 `/v1/status` 从不读 `/`** —— 现在读了,而且我把修复注释掉跑过一遍确认断言会红。<br><br>**第二件:一个 Release 都没发过。** 重写时把 release workflow 丢了,`releases/latest/download/…` 解析到空。推 `v*` tag 重新会构建并上传两个安装器。<br><br>顺带:扩展首装说明还在教人点"More options → Server settings",而重写后的扩展根本没有那个菜单。

**一条命令装完,而且能指向不在本机的 Logue(你说的"一个命令应该就可以把 extension 和 web 都安装""我们需要支持远端服务器……这个链接我们是肯定可以支持的")** —— 两条要求其实是同一件事的两半。<br><br>**一条命令**:`install.sh` 现在用它**已经下载并校验过**的那个包把扩展一起装了,所以 `latest` 只解析一次,两半不可能是不同版本。两条命令意味着一台机器可能只有 Host 没有扩展 —— 而缺掉的那一半正是没法告诉你它缺了的那一半。没有 Chrome 的机器用 `LOGUE_SKIP_EXTENSION=1`。<br><br>**地址**:"一台机器,一个 Logue,一个地址"只在 Host 只能从 127.0.0.1 到达时成立。经隧道发布出去的 Host,地址是任何构建都猜不到的,而写死的那个在 Chrome 里没法改。现在它是面板里的一个设置,存在 `chrome.storage.local`(升级不丢 —— 安装器换的是稳定文件夹的内容而不是文件夹)。**设置放在面板而不是应用里**,因为应用的 Settings 正好在要被指向的那台 Host 后面。<br><br>**先试再存**:存下一个打错的地址会让所有界面指向空气,而退路正是同一个输入框 —— 那时它已经没法告诉你下一个地址好不好。所以 Connect 先去问 `/v1/status`,有 Logue 应答才存。<br><br>两处跟着地址走:远端 Host 不再触发"浏览器落后于已部署构建"的自我重载(Host 看得见的文件夹只有在同一台机器上才是这个浏览器的文件夹);"Logue is not running on this Mac" 改成说出它试过的地址。<br><br>**真浏览器 + 真 https 隧道验过**([remote-host.mjs](../../scripts/qa/remote-host.mjs)),9 项全过。关键的一项是**写入落在被指定的那个 workspace、本机 8787 里一条都没有** —— 两个地址通向同一台机器时,保存成功证明不了用的是哪条路,所以它写进第二个空 Host 再读回来。<br><br>路上修了测试台两个自己的毛病:`browser.sh` 最后一步在 CommonJS 下跑 top-level await,一直解析失败,**每次启动都停在 chrome://extensions**;以及现在可以用 `LOGUE_TEST_EXTENSION` 加载还没部署的构建,不必先覆盖机器上那套 Logue。<br><br>**仓库转公开时清掉一处**:你真实的隧道域名被我写进了单元测试当样例,而那台 Host 没有密码 —— 等于公开点名。提交还没推,已经重写掉,`main` 的整条历史和工作区都搜不到。

**发布 v1.0.0(M2)** —— 你说的 "then create new release"。<br><br>**版本号没按建议的 v0.3.0 走,原因是量出来的**:仓库里四个 package.json **全都已经是 1.0.0**,你浏览器里跑着的扩展也是 1.0.0。发 v0.3.0 会打出一个 `version: 0.3.0` 的 manifest —— **比已装的低,Chrome 直接拒绝降级**,自更新那条链也会一起断。而且按任务里自己的说法,这本来就是"当前架构下的第一个正式版本",不是接着 v0.2.13 往上堆 —— 那套安装早就不存在了。所以是 **v1.0.0**。<br><br>**发布前逐条确认过**:四道门禁全绿(typecheck / lint / 前端测试 / 137+ 条 Host 测试)、队列确实清空、`deploy.sh` 跑过、工作树干净。**产物回读过**:44 个文件、325 KB,`VERSION` 是 `v1.0.0`,manifest 里 `version: 1.0.0` / `version_name: v1.0.0`,`server/logue_host/__main__.py`、`web/index.html`、`extension/manifest.json` 都在,checksums 一起出。tag 已推。<br><br>**要你确认的一件事**:你浏览器里那份扩展**最多 5 分钟自己换到新构建**(X2 那条链今天验过一次)。按 ⌘⇧L 打开面板,看一眼是不是新的 —— **拷进安装目录不等于浏览器在跑**,X19 就是这么栽的。

**真 key 之下全部重验(S3)** —— 7 步里 **5 步跑完并全绿**,2 步是环境限制,已写清楚。<br><br>✅ **转写质量**([speech.mjs](../../scripts/qa/speech.mjs),新,不经浏览器):口水话进去 `um / uh / you know / I I` 全没了,`ship / panel / anchors / later` 一个不少,没加字;静音进去出来是空的。**挖出 X36**(静音被编成一句话)和**测试台一直在喂静音**。<br>✅ **三态 12/12**:真实延迟下的加载、**把模型指到不存在的名字**逼出真 404(用完放回去)、最长答案在对话框里不横滚不出界。<br>✅ **F5 学词 10/10**。<br>✅ **f3b 10/10**(agent 的判断):它先查再答、答案带活引用、要改工作区只提议 —— 并**修了一个真的 agent bug**(让它写文档,它有时候把文档写进回答里,那样你拿到的是留不下来的字;加一行 prompt 后 5/5 走提议)。<br>✅ **v7 8/8**(改写值不值得接受):真模型把 "This passage will be rewritten by the mock." 改成主动语态,逐段接受/拒绝、Apply 落进正文、Run 留痕。<br><br>**这一轮 v7 上我错了三次,三次都是我的检查而不是产品**:①断言找的是**替身的原话**("mock answer standing in for the model"),真模型永远写不出来;②我改完断言让它变绿,但它读的还是**上一份文档** —— 因为 `location.hash = '#/documents/…'` 这行在 F7 把 hash 路由删掉之后就是死的,**创建了文档却从没跳过去**;③换了正确断言之后又踩到真模型的**变化性**:让它"说得更朴素"时它有时判断无需改动 —— 那时对话框说"这次改写不会改变读者看到的东西"、Apply 置灰,**产品是对的**,是检查假设了总会有改动。现在先重试一个必然产生改动的指令,还不动才算失败。<br><br>**剩下两条是环境,不是产品**:`cuj-voice` 的**真语音那半已经由 speech.mjs 覆盖**,浏览器那半只能用 Chrome 自带哔声证明管道(假麦克风喂文件这条路在这版 Chrome 上是死的);`cuj-gdocs` 要你的登录态,得在你自己的浏览器里跑。

**一条 Source 被后来的推翻了,现在说得出来(R13)** —— 你批的是 ②:**模型发现矛盾,建议;你点头才算数**。<br><br>**为什么该由模型先看见**:这恰恰是人看不见的东西 —— 没人记得三个月前那条 Source 说了什么,于是新旧两条都在、都能被引用,旧的那条继续被当成现行的。<br><br>**怎么发现**:归类每条新 Source 时,先用**共同词**从**更早的**Source 里挑出最多 8 条候选(算术,不是判断 —— 和查重复同一个道理),连同"只有当这条让其中某一条**变错或过时**时才填 `supersedes`;两条只是说法不同不算矛盾;拿不准就填 null"一起交给模型。<br><br>**怎么落**:模型给的 id **逐项核对**(存在、不是自己、确实更早、还没被推翻过)—— 编造一个 id 会把"已被取代"的横幅贴到随便哪条上。然后**什么都不做**,只留一个提议。**"归档"和"那条旧的现在错了"是两个决定**,所以是两个按钮:File it 明确不带同意,旁边单独一个 "Mark the older one out of date",还有一个 "Read the older one" 让你先去看。<br><br>**同意之后**:两头都写(读旧的问"这还成立吗",读新的问"它改了什么",一个指针只答得了前一个);旧的那条**不删、不改、不移出 Project** —— 它写下时是对的,那份"当时怎么认为"值得留着 —— 只是**淡下去**并在最上面说明白它被哪条取代了,一键跳过去;**被引用时角标变灰并带一个 †**,因为引用才是它造成损害的地方:一个站在已经不成立的东西上的答案,看起来和站在成立的东西上一模一样。<br><br>5 条 Host 测试(提议不改任何东西、归档可以不带同意、编造的 id 丢掉、**不能被比自己早的推翻**、候选名单只含相关的更早 Source),真机 12/12 + 真模型端到端 —— 模型自己写的理由是 "The review queue limit has been updated from twenty to fifty sources."<br><br>路上踩了两个我自己的坑:①`_mark_superseded` 和 `resolve` **两个写者写同一条记录**,后写的那次把前一次的字段抹掉了 —— 和当初 `draft_document` 那个是同一个病,现在只有一个写者;②改 prompt 那次**替换根本没命中**,而我没检查就去跑了,结果模型压根没收到那段话 —— 我自己记下过这条教训,又犯了一次。

**一个页面一条对话,而且它叫 Chat(X38)** —— 你的原话是"我去 talk 某一个东西,它给我换到其他页面的时候也放在这里……就完全没有相关性一样"。<br><br>**根因就是写死的一个键**:`logue:thread`,一个常量,读写都只认它 —— **和你在哪个页面毫无关系**。所以一篇文章上问的话,换到 Google Doc 照样挂着,正如你截图里那两条根本不相干的内容上下贴在一起。<br><br>现在按页面存([thread.ts](../../extension/src/thread.ts)):**忽略 `#` 之后**(目录里点一下不该把一段对话劈成两半),保留 `?` 查询(对很多站点那就是地址本身),**只留最近 20 个页面**,再老的丢掉 —— 浏览器一天要开很多页,只增不减的存储迟早会坏。回到原来那一页,原来的对话还在。**原来那条全局对话直接删掉**:它没法归给任何一个页面,留着就等于在每个页面上重放同一段话 —— 那正是这个 bug 本身。7 条单元测试钉住(两页互不串、换页回来还在、`#` 不算换页、超出 20 页丢最旧、清空只清这一页、旧的全局那条被丢掉)。<br><br>**Talk 全部改名 Chat**:标签、`aria-label`、`sr-only` 标题、代码里的标识,以及跟着改名的那几个真机检查。<br><br>真机 12/12:在你自己应用的两个真实页面上各跑一个 Skill,面板跟着当前标签页换内容 —— 第一页只看得到第一页的,第二页只看得到第二页的,那条老的全局对话哪儿都不出现。

**设置里不认识的 key 直接拒绝(X37)** —— `PATCH /v1/settings` 原来什么都收:我写进一个 `model` 字段,它照单全收、存下、`GET` 还读得回来,而**没有任何代码读它**(真正管模型的是 `/v1/model`)。设置看起来存住了,行为一点没变 —— 客户端拼错一个字段名,永远不会有人告诉你。这正是它被发现的方式。现在有 `SETTINGS_KEYS`,和 `materials.update` 那套一模一样;`pins` 在名单里,因为**读它的是应用而不是 Host,而一个设置不会因为在乎它的是客户端就不算设置**。装机 Host 上验过:真设置 200,编的那个 400 并把名字说出来。我当初写进去的那个死键也从工作区里清掉了 —— 那是我留下的。

**Google Docs 的选区:你定了不做(X17b)** —— 三条路摆出来之后,你打开了 screen reader support,我量完了:**技术上可行** —— 顶层 `getSelection()` 确实是空的,但双击、三击之后,选中的文字整整齐齐出现在 `docs-texteventtarget-iframe` 里(同源 `about:blank`,主框架读得到),三击那次一字不差。不需要解 canvas。**你说不做,所以不做。** 边界写进了 [behaviors.md](behaviors.md):Docs 上只有语音入光标,没有选区工具条 —— 并且写明**这是决定,不是做不到**,连那条可行的路也记在那里,免得将来有人重新研究一遍再得出"不可能"的错结论。

**别的 agent 也能用 Logue,先做文档这一层(G1)** —— 你的原话是"让任意 agent 能够使用我们的这个产品。第一个是文档吧,假如我把这个文档链接给比如说 Claude Code,它应该有能力读写这个文档"。<br><br>**做出来的东西**:`integrations/claude-code/`,跑一次 `install.sh` 就装进 `~/.claude/skills/logue`,之后在任何项目里 `/logue` + 一个文档链接即可。链接自带一切 —— 哪台 Host、哪篇文档,没有 key、没有配置文件。Host 那边**一行没改**:它本来就放行"没有 Origin 的本机工具",拦的只是网页。<br><br>**真正难的不是调接口,是格式。** Logue 存的是编辑器的 HTML,agent 写的是 Markdown,两边要能互相还原。拿**工作区里全部 49 篇真文档**跑往返,第一版 11 篇被改坏,逐条查出三件事:①块与块之间被凭空插进空行(整篇重写会把人的文档变成双倍行距);②**看着像 Markdown 的纯文字被提升成了真标记** —— 编辑器根本没有标题功能,所以 `## 需求`、`1. 执行前验证`、`* **顺序**` 在真文档里就是字;由生成直接落成的文档更是**一个标签都没有的纯 Markdown 文本**;③尾部的 `&nbsp;`、孤立的 ``` 被吃掉。修法是"只出不进"的转义:出来的时候给这些字加反斜杠,进去的时候脱掉。现在 **49/50 篇往返零差异**(可见文字逐字相同,且再读一次结果不变),23 条单元测试进了 `npm test`。<br><br>**两道防覆盖闸**:整篇替换必须带 `--revision`(工具自己拦),版本对不上由 Host 拒绝(409);追加不需要版本,因为它盖不掉东西。**真机验过**:建文档 → 追加 → 整篇重写 → **在浏览器里用人手改一笔** → 再读 → 再整篇重写,五个版本,标题/有序列表/加粗/行内代码/`[Source n]` 全部原样,截图在应用里核对过。<br><br>**没验到的一条,说出来**:`/logue` 这个命令**在真的 Claude Code 会话里被触发**这一步我没能证明 —— 这个环境里嵌套跑 `claude` 拿不到登录态(`OAuth session expired`)。skill 文件已经在 `~/.claude/skills/logue/`,格式和 `peer` 那个一样;要确认只需你在任意项目里敲一次 `/logue` 加一个文档链接。

**权限框结案(X11)** —— 你说结案。三轮都指向同一个结论:**不是 Logue**。①代码全树扫,页面侧一次都没直连过 Host;真机对照(装/不装扩展)数页面发出的私有地址请求,两次都是 0。②在你截图那个站上,加载后、面板打开后、等一段时间后各数一次,全程 0,`local-network-access` 始终 `prompt`。③在**你自己的浏览器**、真实文章页上走完选区那条链最深的一步(工具条出现、跑 Skill、答案带 Insert 回来),三个口都挂了钩,私有地址请求 **0 条**,没有任何提示框被触发。**留着的那句话**:它要是再出现,只需要告诉我**弹框跳出来那一刻你刚做了什么** —— 有这一句就能重开。

**存过的选区,能回到它在页面上的位置(P6)** —— X18b 清单里唯一**今天完全没有对应物**的一条,你说做。<br><br>**锚是三个字符串**:那句话本身,加上它前后各约 48 个字。**不记任何结构** —— 不记 CSS 路径、不记节点序号、不记字符偏移,因为那些是最先坏的东西。站点改版、换个框架重渲染、外面多包一层 div,句子还是那个句子。单元测试把这条钉住:同样的话换一套完全不同的标签,照样找得到;换行重排,照样找得到;**同一句话出现两次时,靠记住的邻居挑对那一个**。<br><br>**四种状态都是真的结果,不是装饰**:①**已锚定** —— 面板里 "Find it on the page",页面滚过去并把它选中(用 `scrollIntoView` 而不是 `window.scrollTo`:很多页面根本不滚窗口,包括 Logue 自己的应用,滚窗口等于什么都没滚 —— 这是真机上抓到的);②**页面变了** —— 找不到就明说找不到,**不猜一个差不多的**;③**已重锚** —— 你在页面上选中它现在所在的位置,一键把指针指过去;④**只有快照** —— 这不是失败,话本来就存在 Source 里,把这件事说清楚,好过一直给一个按不动的按钮。<br><br>**锚单独存一个字段,不进 `source`**,这是整件事的分界:**出处不可改,指针可以修**。真机 15/15,验的时候把页面上那句话**真的删掉**再看它怎么说。<br><br>**当时有一处没验到,现在补上了(P6b)**:P6 那条测试段落在页面上始终在可视区里,所以"滚过去"这个动作本身没被证明 —— 我没有拿一个滚不动的页面去假装证明它。`scripts/qa/p6b.mjs` 换到真实的 Wikipedia《Special relativity》上重跑:页面 48240px 高、窗口 757px,段落在 31539px 处;先断言 `scrollY=0` 时它**在视野外**,再 locate,页面滚到 31190,段落落在 349px 并被选中。**12/12。** 顺带抓到一个真问题:https 页面根本 fetch 不到 `http://127.0.0.1` 的 Host(混合内容),断言必须从面板那边发 —— 第一次跑把它报成了功能坏掉。

**静音不会再被编成一句话(X36)** —— S3 第一步就撞上,**这是这个产品最不能有的失败**。给 Host 灌五秒纯静音(数字零),真模型回的是 `To calculate the standard deviation, start by finding the mean of the dataset, which is just the average.` —— 流畅、自信、没有人说过,而 Logue 会把它打进你的光标。两秒回空,五秒就开始编。**替身模型永远看不见这一条**:它照触发词返回固定文本,不会瞎编。<br><br>两层修法,因为 prompt 是请求,麦克风是事实。**prompt**:其余每一条指令都在推着模型产出文字(Skill、词表、纠错、光标周围那段引文),**没有一条说"不产出也是合法答案"** —— `NOTHING_WAS_SAID` 说了,排在最后、在 Skill 之外,所以谁都改不掉它;测试用一个写着 "Always produce a full sentence" 的 Skill 钉住。**录音端**:麦克风听没听见,浏览器自己能量,`AnalyserNode` 一路记峰值,低于 0.02 的录音**根本不送去问模型**,工具条直接说没听见。只有明确的 `false` 算数 —— 旧版录音器不回报这件事,而一个没被回答的问题不该变成指控。真模型复验:三次静音,三次空。

**测试台一直在喂静音,而且不出声(测试设施)** —— Chrome for Testing 151 收下 `--use-file-for-fake-audio-capture` 却什么都不放:`getUserMedia` 成功、`MediaRecorder` 照写 WebM、**峰值 0**。单声道、双声道、48k / 44.1k / 16k、仓库内外,全试过,全静音;把这个参数拿掉,Chrome 自带的假设备给出峰值 1.03、五秒 74KB —— 管道没问题,是文件从来没播。**没有任何一步失败,所以一直没人发现**,而模型又会把静音填成句子 —— 于是"模型听错了"其实是"根本没有声音"。现在 [mic-level.mjs](../../scripts/qa/mic-level.mjs) 是所有浏览器语音检查之前的闸,[speech.mjs](../../scripts/qa/speech.mjs) 绕开浏览器直接把真人语音喂给 Host。**诚实的话**:此前经这个测试台声称"真语音真模型"的转写结果,证明不了。

**面板里的条目能改了(D2)+ 选区上的答案只有一个去处(D3)** —— 这两条**你早就批过,我却一条都没做,也没写进这个文件**,是被"丢掉"的那两件。<br><br>**D2**:面板 "This page" 里每一行展开后,最上面是**这条到底写着什么**,可以直接改 —— 离开输入框就存,Esc 还原,清空不算数(一条没有字的 Source,派生出来的东西就指向了空)。原本只能看:在这一页上听错一个名字,得跑回应用去改,而这趟路没让这次修改变好一点。<br><br>**D3**:从选区工具条上跑 Skill,答案**进面板**,页面上不再展开第二块。原本同一个 Skill 从右键菜单跑就进面板、从工具条跑就盖在正文上 —— 而盖在正文上那份还会随选区一起消失。两条路现在都走同一个 `runSkillIntoThread`。<br><br>**⌘⇧U 一起收了**:它当时还在开页面上那个 ask 框 —— 正是 D1 说要搬进面板的那一个。搬完之后那个框只剩它一个调用方,于是 `CommandBox` 连同 `visible.ts` 里的 "command" 面一起删掉;面板输入框本来就有麦克风,能力一件没少。<br><br>真机 15/15([d2d3.mjs](../../scripts/qa/d2d3.mjs)),真模型:选中 Karpathy 那条 1,605 字的真实 Source,Simplify 的答案落在面板里,页面上没有 Insert、没有 "N Sources"。改一条的正文,回读 Host 确认存住了,然后**测试把它改回去**。

**"Starting mic…" 转到天荒地老** —— 你说是旧扩展,不用管;但同一个洞在**当前代码里也在**,所以顺手补了。`sendMessage` 自己不带超时:发给一个永远不回话的监听者,它就永远不 settle。而 `getUserMedia` 在系统举着麦克风授权框、或者别的应用占着麦克风时,恰恰就是不回话 —— 屏幕上只剩一个转圈和一句 "Starting mic…",没有任何东西能结束它。现在 `toOffscreen` 跟一个 15 秒的期限赛跑,`start()` 明确回报麦克风起没起来,选区工具条无论哪种结果都会离开 starting 态。**能拒绝的状态人可以离开,沉默不行。**

**面板专项审计(A1)** —— 照 web 那次的量法(计算样式、真实内容、三个标签逐个),不靠眼睛。量出五条真的,全修了:①**点击区域低于 24 像素**三处("Open Logue web app" 22px、行里的两个按钮 16/17px)—— 用同一个手法撑到地板(内边距撑大、负外边距还回版面);②**11px 又冒出来一处**(T1b 刚把这一档整个删掉);③**面板一个 `h1` 都没有** —— web 五个路由在 T1b 修过,面板漏了,现在当前标签就是它的标题(读屏能听见,屏幕上不重复画);④⑤一个 16px 纯黑的元素 —— 追下去是 `<title>`,在 head 里、根本不显示,**是我的探针把它算进界面了**,不是产品问题,探针已限定在 body。复量:三个标签 `small: []`、无 11px、无杂色、不横向溢出。


**面板空态统一(Q1)** —— 四条一起:①Talk 空着时不再是"一整屏白 + 底部一行小灰字",而是**居中的一句话**;②那句话和输入框 placeholder 原本几乎一模一样、上下挨着说了两遍,现在只说一次;③三个分区原本三种空态长相(顶到底部的灰字 / 折叠行 / 顶部一长段),现在共用一个 `NothingYet`;④**错误提示从 `justify-end` 容器里挪到顶部** —— 原本它会被压到贴着输入框的位置,一条要人看见的消息不该在角落里小声说。真机 5/5。


**音频搬到你说的那一行(X35)+ 链接通到 Stream 里那一条(X34)** —— 两条都是我做 Q2 时做偏的,你指出来后改回你原本的意思:①音频条现在占的是**域名腾出来的那一行**(在正文上面),不是原来的正文下面;②那个外链按钮从"打开它当初来自的网页"改成 **"Open this in Logue"** —— 打开它在 Stream 里的那一条,带着血缘和它派生出的东西。真机 4/4。<br><br>**过程里我又弄坏了一次**:把音频搬上去的同时,**整行的正文被我删掉了** —— 每条只剩时间和按钮,而这是一个"存了什么"的列表,连东西本身都不显示。更糟的是我"补回来"那次替换锚点没命中、**静默失败**,我还照常部署,是探针在真机上抓出来的(行里 `<p>` 数量为 0)。**教训**:一次替换有没有生效必须当场断言,不能只看类型检查过了 —— 类型检查对"少了一段 UI"这件事什么都不会说。


**Source 页重排:原始在上,派生在下(F11)** —— 顺序原来是反的:**转写**(派生出来的东西)占着最显眼的位置,而真正不可变的两样 —— **录音**和**页面上那段原文** —— 一个在中间、一个掉在最底下。现在最上面是一块 **"What this came from"**,里面把两样分开写:**The recording**(带真实时长)和 **The passage on the page**(原文 + 出处链接),两者来路不同,不混成一堆。转写和其余的排在它们下面。这跟产品最底下那条规矩是一回事 —— **原始不可变,派生指回原始** —— 界面的顺序现在把它说对了。真机 5/5。


**`⋯` 菜单向 Claude Code 那个看齐(X33)** —— 五条照你圈的:标题行缩成一行小灰字(不再占最显眼的位置)、行高 24px、菜单收窄、**重蓝框换成轻底色**、每项右侧显示字母,而且**按下字母就执行**(P / O / C / D / R)。改在共用组件上,所以边栏、Skill、文档三处一起变。**在你的浏览器里验的**:右键一行,菜单是新形状;按 `P`,"读书"跳到列表第一行并带上图钉,菜单自己关掉。


**音频时长不再是 0:00(X31)** —— 根因就是你队列里写的那条:`MediaRecorder` 边录边写,**从不回头补时长字段**,所以浏览器读不出来。修法也照你写的:**录的时候我们自己就知道** —— 停下那一刻的秒数随音频一起送到 Host,存进 capture 的 sidecar,并跟着 Source 走(重试转写也不会丢,它从存好的地方读)。界面直接显示这个数,不解析、不猜、离线也对。**顺带把 Range 请求做了**(`bytes 0-99/1133`,`Accept-Ranges: bytes`)—— 没有它,十分钟的录音连拖动进度条都做不到。<br><br>两处播放器本来一个带 `preload="none"` 一个不带,**现在收敛成一个 `Recording` 组件**,面板和应用共用 —— 和 F10 是同一条道理。真机 5/5:新录一段,时长显示 `0:03`(以前 `0:00`)。


**面板重设计(D1)+ 离线录音队列有了家(P1)** —— 问答搬进面板、三个分区标签(Talk / This page / Project)、对话改成输入在下消息在上;✦ 不再在页面上弹框而是开面板。等待中的录音哪个标签下都看得见,每条说清什么时候录的、多长、在等还是失败了几次,三个动作:重试 / 导出音频 / 删除 —— 为此队列学会了两件它从来没记过的事(时长、失败次数)。真机 11/11。

**Q2 四条 + 批注连原文一起复制(F8)** —— ①**扩展从来就没有图标**:manifest 里 `icons` 和 `action.default_icon` 都不存在,所以 Chrome 在面板标题栏、工具栏、扩展列表里画的都是灰底占位方块。不是"logo 没渲染",是我们没给过。用产品真实的标识(accent 方块 + 声波)出四个尺寸的 PNG,并让写 manifest 的同一段脚本把文件一起搬进 dist —— 声明指向一个构建忘了拷的文件,比不声明更糟,Chrome 会拒绝加载整个扩展。②Chrome 自己的面板标题栏已经写着产品名和图标,我们下面那一行是第二个,删掉;右上角改成 **"Open Logue web app"**。③两个分区合成**一个列表**按时间排 —— "从这一页存的"和"你补充的"是同一个问题(这里有什么)的两个标题。④每行重复的域名去掉(整页十几行说同一件事十几遍),那一行改放**音频条 + Link + Copy**。<br><br>**Copy 就是 F8**:复制出去的内容里,**原文在上(引用块)、你的批注在下、末尾一行回到它在 Logue 里的位置**,同时写纯文本和富文本两份 —— 粘进 Notion/Docs 带格式,粘进纯文本框也不会变成一堆符号。批注本来就是独立的派生资料、`parent_ids` 指回原文,所以这一对本来就存着,复制只是把它们拼出来。真机 7/7。<br>**格式还没经你过目** —— 按"拿不准先提方案"那条,我照队列里写好的提议做了并留在这里,你看着不对随时改。


**两条浮层收敛成一套(F10),连同它统的四条** —— 选中文字那条和光标旁那条本来各写各的,已经差出来了。现在有一个共用外壳 [FloatingBar.tsx](extension/src/surfaces/FloatingBar.tsx):同一个框、同一个把手、同一套拖动与方向键微调、同一句提示语。**改一处两边一起变**,这正是你要的"不能只改一个地方"。落进去的四条:**X29** ✓ 落在麦克风原来的位置(中心坐标一致,鼠标不用动)、**X32** 把手一直可见且**选区条第一次有了把手**(它以前一个都没有)、**F9** 按住条身就能拖(把手只有 14px,还可能被页面盖住)、**X30** 插入后**只剩 ↶**,并且 5 秒 / 点别处 / 开始打字 三者任一就自己走。<br><br>路上抓到一个真 bug:选区条为了不丢选区**每次按下都 preventDefault**,而我原本拿 `defaultPrevented` 当"别人已处理"的信号 —— 等于恰恰在那条最需要的浮层上,把"按住条身拖"也关掉了。改成:自己的判断先做,调用方照常跑。<br><br>真机 4/4 + 6/6,**X30 这一组是真语音真模型**:用 `say` 合成一句话喂给假麦克风(`scripts/qa/spoken-loop.wav`,五分钟循环),Gemini 转出原话再落回光标。为了走到这一步修好了三件测试设施:`browser.sh` 依赖的两个安装脚本当初搬仓库时漏了、假音频放完就静音、以及**录不到一秒就点采纳**(片段太短模型听不出东西,看起来就像功能坏了)。

**地址里不要 #(F7)** —— 早些时候已随 X26 一起做完:`/documents/doc_1a2b`,pushState 走、popstate 回,旧 `#/` 书签解析一次后重写。真机 11/11。这一条在队列里出现了两次(两个会话各记了一次),现在合并。


**改完代码页面就坏(X27)** —— 不是"变旧",是**直接坏掉**:构建产物按内容哈希命名,重新部署会删掉旧文件,你那个开着的标签页还在要它们。扩展早就会自愈(X2),web 这半一直空着。修法按规则来 —— 不请 dev server:`/v1/status` 里本来就有 `build`,页面记住自己加载时那一个,每 20 秒问一次,**变了就自己刷新**。**但绝不带着没保存的字一起走**:有未落盘的编辑时,它改成在底部说一句 "A newer Logue is ready",给 Reload 和 Not yet 两个按钮,由你决定。真机 6/6。路上我的探针错了两次:先是只派发 `input` 事件却没真改内容(编辑器没排队保存,理所当然),然后是等了 20 秒 —— 而自动保存**一两秒就落盘**,那时确实没有未保存的东西。第三次把保存请求挂住,才真的到达"话还在你这台机器上"的那个窗口。**一个取舍留给你**:这不是 Vite 那种保状态的 HMR。要改一行 CSS 页面不闪、状态还在,就得把 dev server 请回来,而那和"一个地址"那条规则冲突 —— 说一句我就改方案。


**边栏预览卡崩掉整页(X28)** —— 根因不在预览,在**同一种记录有两个地方在写**:agent 的 `draft_document` 自己手搓了一条 document,绕开 `documents.create()`,于是少了 `source_ids`,而预览卡直接读 `source_ids.length`。工作区里那条 "A mock draft" 至今缺这个字段,拿它做的反事实一字不差:`TypeError: Cannot read properties of undefined (reading 'length')` —— 就是你贴的那句。**三层一起修**,少一层洞就还在:①agent 改走 `documents.create()`,写记录只有一个入口;②四个预览里同样的写法只有一个做了防备,现在都不假设数组存在;③**预览卡加错误边界** —— 一张描述某一行的卡片,不该有能力把整个应用拆掉,不管数据怎么坏。现在它显示 "This one cannot be previewed. Open it to see it." 并把错误记一次。**在真实那条记录上验的 6/6**:它还在、划过去页面不倒、什么都没抛、卡片正常显示、新起草的文档带着 `source_ids`。


**X10 的第二扇门(X27)** —— 十分钟上限触发后,麦克风确实关了,但"我在忙"**永远为真**:`recorder` 对象只有 stop/cancel 会释放,而"忙不忙"判的是这个对象在不在。页面一旦走了(关标签、跳走、内容脚本没了),没人来收那段音频,**这个浏览器就再也收不到新构建** —— 就是你 04:37 报的那句 "Already recording." 背后的同一个病,换了一扇 F4 开的门进来。我是踩上去才发现的:上一轮走查里我的选择器误点了"Voice comment",录音起来、页面跳走,测试浏览器从此不换代;先以为是自己手滑,量完才知道手滑只是触发条件。**改法是把一个问题拆成两个**:`recording()` = 麦克风是不是活的(所有问它的人真正想问的是"现在打断会不会让人丢话");`holding()` = 有没有没人收的音频(关文档会把话扔了,所以不关)—— 而后者**自己会结束**:上限之后 3 分钟没人来收就放掉。最坏情况从"永远冻住"变成"多等一两次检查"。5 条单元测试钉住状态机,并且**做了反事实**:换回旧写法,那条测试立刻红。真机侧的证据是这个修复自己送达的过程 —— 测试浏览器当时正卡在旧构建上,手动解开死锁后才吃到新构建。

**你浏览器上的自更新,又验了一次(2026-08-09 09:36)** —— 09:31:47 部署,**09:36:16 你那个开着的真实 Notion 页面已经在跑新构建**(`20260809T093147Z.7f095922`),`ready=1`,没有任何手动 reload。X10 → X18 → X19 那条链在你自己的浏览器上还活着。同时注入面在真实 Notion 上正常:跟着真实光标,工具条是 Move / Voice / Ask Logue / Voice options。**诚实的缺口**:面板文档(sidepanel.html)我在你的浏览器里打不开 —— 浏览器工具会把 `chrome-extension://` 改写成 `https://chrome-extension://`,而 Chrome 不允许没有真实点击就开侧边栏。所以 E1–E3 那三条排版修复只有测试浏览器里的实测,你按一下 ⌘⇧L 我就能在你这边量。

**竞品扫描(R12)** —— 四类看完:随处语音输入(Wispr Flow、superwhisper)、文档里的 AI(Notion)、带出处的知识管理。结果在 [proposals.md](proposals.md),每一条都标了该不该拿。**该拿的只有一条 —— 会自己长的个人词典,就是 F5,已经做完。** 语音快捷短语、团队共享词典、跨设备同步、Notion 的"Agent 自动改工作区"、跨页面 AI 块,五条都不拿,理由逐条写了。其中最重要的一条不是功能而是红线:**Notion 的 Agent 敢自动改东西,是因为它不承诺出处;Logue 承诺,所以任何"AI 动了什么"都必须先回答怎么留痕** —— F3 的"先问后写"就是这条红线的具体形状。**真正缺的一条**:Source 的时间维度(谁推翻了谁),拆成 R13 等你定一句话。

**Side Panel 对 v1 的清单(X18b)** —— v1 面板从 git 历史里读完(归档前最后一个提交,十个文件),列出它会做的 **102** 件事,逐条对今天这个,产出 [panel-v1-gaps.md](panel-v1-gaps.md)。六条值得补、五条建议不补,都写了理由,按你的规矩没有动手 —— 只有 P5 那条是真 bug 不是缺功能,当场修了:面板的 Esc/Enter 挂在 window 上没有守卫,在提问框里按 Esc 会取消录音、按 Enter 会抢在换行之前采纳(v1 有这个守卫,重建时没带过来)。真机 4/4。

**⌘⇧K 与面板里的 agent(F3)⚠️ 判断质量打 mock 标** —— 清单九条全落地。**前三条**:⌘⇧K 一下,面板打开并立刻开始录 —— 它替掉的就是"先开、再找麦克风"这两步,所以绝不能又变成两步;面板是被"请求"打开的、自己决定什么时候到,所以意图先留在它一定会看到的地方,读到即消费(以后再开面板不会平白开始录音,这条验过)。Esc 取消不留痕,Enter 采纳,这段话作为**你自己的消息**进对话 —— 键和页面上那条工具条一模一样,不用学第二套。**后六条**:agent 在 `domain/agent.py`,prompt、工具、循环都在我们手里;**读随便读,写一律先问** —— 找 Source、跑 Skill、读这一页不改任何东西,存页面/归进 Project/起草文档会改工作区,所以它只能**提议**,提议摆在对话里等你点 "Do it"(量过:提议前 28 篇文档,等待期间还是 28,你点完才 29);**它做过的每一步都列在答案上方**,用人话("翻了你的 Sources —— 找到 6 条");答案带活的 `[Source n]`;Skill 读的还是 F1 那一份 contexts,没有第二份清单。顺带把面板里两种问法并成一种 —— 原来输入框跑一个 Skill 把答案印在自己下面,说话进对话流,同一个问题的两个答案互相不知道对方存在。路上逼出两个真 bug:①共享搜索是整句匹配(对人在 Find 里打字是对的,对 agent 把问题当查询是错的)—— 现在 agent 先整句、再逐词,而不是为了一个调用方去改所有人的 Find;②搜不到就一直搜同一句,四步全耗光还没答案(我看着它发生的)—— 现在重复的查询会被挡回去。替身模型学会了说 agent 的 JSON,否则动件最多的这条链恰好是没 key 就走不通的那条。137 条 Host 测试,真机 10/10。**它选得好不好要等真 key**(S3)。

**越用越会拼你的词(F5)** —— 按批过的方案落地,核心判断一句话:**只从人做过的决定里学,永不从转写结果里学。** 一个名字被听错十次,它就在转写里出现十次 —— 按频次学等于把错的当成对的,然后一直错下去。一档:你手动改过的词直接学(改的那一刻你已经用行动说了它怎么拼),并且存进一个比纠错列表活得久的词表 —— 纠错只留 40 条,今天改的名字到冬天就没了。二档:你亲手写过 ≥3 次、而转写从没产出过的专名,进 Settings 的建议列表**等你点头**;找它用了两遍扫描,因为句首的大写什么都说明不了 —— 第一遍只认句中大写的词,第二遍才数它的全部出现,否则每次都当主语的名字永远被漏掉。三档:只在转写里出现的词,永不学。**没有任何词是悄悄学进去的** —— 每个词都写着它为什么在这、都能一键拿掉(你说这条不商量)。prompt 里两层:全局在下、Project 在上。诚实的边界写在模块里:建议扫描只认拉丁字母,中日文名字没有大写可认,走纠错或 Project 词表。130 条 Host 测试,真机 10/10。

**Transcription Skill 生效了(F6)⚠️ 清理质量打 mock 标** —— Skill 本身随内置发货,prompt 就是你要的四件事(删语气词、删重复与自我纠正、整体不动、能简则简)加三条红线(只删不加、不换更书面的词、不替你把话说完);**但你的工作区的转写槽位一直指着另一个逐字版 `Accurate transcription`,所以它从没生效过** —— 现在槽位指向它了。真机 5/5:在 Logue 自己的长文档里真录一段,**82,149 字节**真实音频走完全链,Host 冻结在这条 Source 上的计划里,写的正是这份 prompt 的原话。加了一条 Host 测试把四件事和三条红线逐句钉死 —— prompt 是这个仓库里最容易被"顺手改通顺"改坏的东西。**清理出来的话干不干净要等真 key**(S3):替身模型只回它那句固定的话。逐字版 `Accurate transcription` 保留,想要一字不改时还能选它。

**审计五条清账(T1b)** —— faint 令牌删除(和 muted 差 3 个 RGB 点,屏幕上不存在的区分);11px 档整个删除 —— 它是一个决定的两种拼法(`--text-meta` 令牌 + 散落的 `text-[11px]`),Projects 因此比 Stream 小一号;现在正文 13、说明 12,没有第三档,文档编辑器自己的排版除外。h2 一个字重(560);h1 就是打开的那个条目,分区名是旁边的普通返回按钮,读屏不再把 "ProjectsAgent Harness" 念成一个词;Stream 的主区域补上缺的 h1(念出 Source 的 kind)。照审计自己的量法复量:四个路由真实内容打开,无一处 11px,8/8 + 5/5。第一遍还栽在审计警告过的坑里 —— 空态下量了两个路由,重量。

**真路径 + 进区即新建(F7+X26)** —— 地址栏里再没有 `#`:`/documents/doc_1a2b`,pushState 走、popstate 回,Host 的 SPA 兜底让任何深链接冷加载也落对;旧 `#/` 书签解析一次后重写成无 # 的地址,不多占历史。点 Skills/Documents/Projects 直接落 `/skills/new` 这样的**真编辑器** —— 列表明明在边栏,"从列表里挑一个"的页面让人白点一次;不碰不落盘照旧(量过:前后都是 15 条),Stream 和 Settings 是诚实的例外。Side Panel 里唯一一个 `#/` 链接一起改掉。真机 11/11:落地、旧书签、冷深链、两个草稿、零落盘、rail 行真地址、Back 按刚读过的页面一步步退。

**选区改写,逐段点头(V7)⚠️ mock 标** —— 你拍的板落成了这样:**模型可以碰文档,但只能经过你的手,一次一段。** 选一段、说要怎么改,提案回来时已经折成"决定" —— 没动的段落淡着,每个改动两边都摆出来,默认要、点一下变不要;Apply 落下去就是一次普通编辑,版本历史照记,提案本身存成一条 Run(选区和指令都在),日后"这句怎么来的"有处可查。Host 侧钉死:出提案不碰文档,内容和版本号原样。119 条 Host 测试,真机 8/8。我自己的检查又栽一次:接受和 Apply 挤在同一段同步脚本里,Apply 读到的还是没提交的旧状态 —— 真人两次点击之间永远隔着一次渲染,测试也得隔着。

**页面上的听写不再多问(X20+X21)** —— 说完直接落光标,没有"要插入吗"那一步;"已插入/撤销"并进工具条,不再是一条压着正文的独立浮层。在真实 Google Docs 上得的证。

**Skill 建得出来了(X22)** —— 创建只要名字;instructions 到 Skill 页上再填,页面会明说它还缺什么才能跑。之前是要一个屏幕上不存在的字段,一个 Skill 都建不出来。

**空分区收成一行(X23)+ Find 的重复边框(X24)+ 一套宽度(X25)** —— Side Panel 里空的分区只剩一行标题;`Input` 有了 `bare` 变体,Find 里那圈永远亮着的 focus 边没了;三档页宽收成一套,Settings 里那层多余的 560 一起拆掉。

**全面走查(T1)** —— 产出 [audit.md](audit.md):六条值得修的按值不值得排好,三种极端状态(加载/出错/超长)在替身模型下补齐 7/7。#3(16px 的点击区域)已修到 24px 地板;余下五条在队列(T1b)。

**重建(R1–R11)** —— 归档、功能分级与十条使用路径、脚手架与四道门禁,然后 server、UI 包、web、扩展、安装,十条路径真机跑通,三轮复核,最后删掉归档的旧树。

**一台机器一个 Logue(M1)** —— Host 自己托管应用(`http://127.0.0.1:8787`),并且是会自动重启的登录项;v0.2.13 那套安装、它的十个 release 和它的登录项都清掉了。你的原话是 "there should be just one version in code and running service/extension … installed/running so that i can use/check anytime"。

**文档** —— 能读能回滚的版本历史(V5)、模型写出每个版本改了什么(V2)、标题在没人取名之前自己取名(V4)、打开的是哪一篇写进 URL(V8)。这三项来自 vibedoc 复核(U4)并经你确认。

**Skill 版本浏览(B3)** —— Skill 页上的 Revision 数字现在能点进去:每个旧 prompt 能读、能看逐行 diff、能回滚。回滚写成新版本,Run 记下的版本号永远指向还存在的 prompt。历史对话框和文档共用一个组件,Host 侧的 diff 算术也收敛成一份。

**不碰过就不落盘(X7)** —— 连点 `+` 五次不再留下五条空记录。你的原话是 "if a new doc/skill/proj is never touched it should not be saved"。

**左边栏** —— 分区的列表搬进去,仿 chatgpt.com(U2);改回平铺、每行左侧加类型图标、去掉看不懂的蓝点和图标按钮(U6);学会一个边栏该会的事,取自 agentrunner(U3);再做减法、对齐、hover `+`(U5);把只做了一半的补齐 —— Skills 的 `+`、不再是死胡同的空态、hover 压过 selected、鼠标能移进去的预览卡(U7)。

**外壳(U1)** —— 产品标识、每个路由一条固定顶栏、可折叠可拖宽并且都记住的边栏。你的原话是 "新的 UI 没有 header,也没有 Product logo … 参考第一版"。

**采集与 Side Panel(B1–B19)** —— 自动归组、转写修订与纠错、Side Panel 五个分区、血缘、删除影响预览、备份与恢复、⌘K、自动归类与复核队列、冻结的转写上下文、Tag、采用与撤销、默认 Skill 槽位。

**报过并修掉的 bug(X1–X9)** —— 同屏两个浮层(X1);扩展要手动 reload(X2);第二个写入者静默覆盖文档(X3);验证脚本写进真实工作区(X4);错误提示跟着你去下一个输入框(X5);标签页停在已被替换的构建 —— 这正是选区工具条下面还压着一个输入条的原因(X6);没碰过的新建项落盘(X7);一条长 Source 把整页撑到 6718px、右边一大片死区、每行被切断(X8);Sources 每行点不进 Stream(X9)。

**扩展韧性(B14)** —— 标签页自愈、Host 不在时录音进队列并在它回来时自动送达、选区上的 Skill 前两个直出其余进 `⋯`。顺带修掉一个陷阱:录音器卡住之后界面上没有任何出路。

**自更新死锁(X10)** —— 你 04:37 报的 "Already recording."。抛这句话的代码 B14 已删,但真 bug 在更深处:录音结束后 offscreen 文档没人关,而自更新见到它就让路 —— 录过一次音,浏览器就再也收不到新构建,修好卡住录音器的那个构建恰恰因此进不来。现在录完就关文档、检查时空闲文档先关再继续、真在录音仍然让路。隔离真机 8/8:录音中不打断、录完文档即关、下一次检查即换代、换代后录音干净。

**面板被更新打死(X18 前半)+ 第二地址(X13)** —— 你截图里那个 "Your file couldn't be accessed"。文件一直都在;是 reload 把已打开的面板文档拆了,框还在、里面空了,而面板里本该自救的代码跟着文档一起没了。内容脚本从一开始就有自愈,面板漏了 —— 它今天才现形,正因为 X10 把 reload 修活了。现在 worker 从 reload 回来会重新指向面板,路径带一个计数(不是构建号:手动 Reload 和半截部署都不会改构建号,路径不变就什么都不会发生)。真机 3/3。同时:面板里的"在 Logue 里打开"原本指向 dev server 的 5173 端口,只有在有人开发时才活着 —— 改成 Host 自己的地址,全树再无第二地址。诚实的边界:Chrome 不允许没有真实点击就打开侧边栏,所以"框自己活过来"是从路径变化推出来的,没有亲眼看着它复活。

**边栏全列 + 一个 Find(X12、X14)** —— 边栏原本在十二行上面印着 "Pinned" 和 "Everything else",再把其余的折进 "9 more":两个标题把顺序已经说过的话又说一遍,而折叠把行藏在了一个专职当导航的列表里。现在置顶排最前、一条不截、长了就滚(127 条 Source,127 行)。同时 Find 上面本来还压着第二个搜索框,只筛当前这一段列表 —— 窄的那个看起来才像答案,所以拆掉了;而 Find 自己也名不副实:搜 Source、文档、Project,唯独跳过 Skill —— 偏偏 Skill 的每一行都是你亲手写的 prompt,最难靠滚动找到。现在它搜,并且点得进去。真机 9/9。路上我自己两个测试 bug 都是"过了但什么都没证明":先用只标在高亮行上的 `data-at` 选行,于是只回来一条,被读成"Skills 没搜到";改完又用 /Skill/i 匹配,随便哪行的文字都能满足。现在按 Skill 全名断言。

**CommandBox 的麦克风(V6)⚠️ mock 标** —— 你批的那条,V3 落地后它才成立:问题可以说出来,词落进**指令框**而不是页面,和已经打的字并排(逐次到达、只追加一次),转写还在路上时框已经能接着编辑。真机 5/5。

**异步听写(V3)⚠️ mock 标** —— 量出来的结论(3 秒话等 3.4 秒、30 秒等 10.7 秒、长音频更久)判了"锁住等转写"的死刑。现在 stop 分成两半:**收音是独占的、一瞬间的;落稿是后台的、可并发的**。音频一到手麦克风立刻归还,新录音马上能开,每一段落在**它自己**冻结的光标上(共享的落点会被第二段录音覆盖 —— 改成逐段闭包);空闲条上有个小计数说"还有 n 段在转"。隔离真机 5/5:两段录音重叠飞行、转写未归麦克风已回、两段各落各位、计数归零。Notion 真页复验:27.7 万字节真实音频落进块里。顺带按你的话把回执的字全删了(✓ ↺ ✕,悬停出说明)。取消只取消**录音**,永不取消已在飞行的落稿。诚实注records:V3 的代码搭了"去字幕"那个提交的车,提交信息只写了后者 —— 下不为例。

**OpenAI 兼容 provider 就位(F7 代码半边)** —— Groq 和一切说 OpenAI 格式的免费档,一个 provider 类全包;Settings 里有了 Provider 选择(Gemini / OpenAI-compatible (Groq)),Groq 的地址和模型是默认值,**key 一贴就能用**。模型路由改成"合并到存档记录"而不是改实例 —— 否则 mock 在场时每次保存都会忘掉你选的 provider;切换 provider 会重置 base_url/model,免得 Gemini 的地址漏进 OpenAI 路径只换来 404。wire 格式用"请求记录器"钉死(Bearer 头、system+user 消息、multipart 的 prompt 字段带我们的转写计划),语音健康检查发一段真实的四分之一秒静音。116 条 Host 测试,真机 5/5。**剩你的半边:在 console.groq.com 建号拿 key**(或重新申请 Gemini key),贴进 Settings。

**Google Docs 真机通了(X17)⚠️ 转写内容打 mock 标** —— 按你的原话在你的浏览器里**新建了测试文档**(Drive 里叫 "Logue Test — Google Docs"),整条链在真实 Docs 上走完:光标旁出现麦克风 ✓;真实麦克风录音,**543,581 字节**音频走完 内容脚本→worker→offscreen→Host ✓;转写文本(替身)经 beforeinput 落回光标原处 ✓;X20(无二次确认)和 X21(Inserted/Undo 在工具条上不压正文)在真实 Docs 上同步得证;F4 的计时也在。**没通过的一半是诚实的边界**:Docs 的选区画在 canvas 上、DOM 里没有 selection,选区工具条和选区 Skill 在 Docs 上今天不存在 —— 拆成 X17b 等你拍板。真 key 到位后按 S3 重验转写内容。

**答案能打开,Source 能进去(X15)** —— Recent answers 里每一行原本印着 Skill 名、`28 Sources`、时间,全是死字:那二十八样东西你一个也够不着,答案本身也没有地方可读 —— 那是工作的记录,不是工作。现在整行打开答案,引用是活的;底下列出它依据的每一条 Source,一键就到它在 Stream 里的位置。真机 5/5。我自己的测试选择器又栽了一次:按"数字加空格"开头找行,而行的文字是 `1某某内容` 没有空格 —— 在一个明明列着 28 条的对话框上报了 0 条。现在那个列表是真的 list、有真的 label,检查读它的方式和读屏软件一样。

**快捷键真的绑上了(X16、F2)** —— 你问 "why the shortcuts are gone"。查了 v1:三个命令(⌘⇧Space 语音、⌘⇧M 问这一页、⌘⇧L 开面板)现在的 manifest 里一个不少,web 端还比 v1 多出 ⌘\、⌘1–5、⌥⌘↑↓、右键菜单、⇧F10、`?`。但**声明不等于绑上**:在只装了 Logue 的全新 profile 里问 Chrome 自己(`chrome.commands.getAll()`),**⌘⇧M 绑的是"什么都没有"** —— macOS 上那个键 Chrome 自己留着开个人资料菜单,它拒绝得悄无声息。改成 **⌘⇧U** 并实测绑上了。同时 ⌘⇧L 只会开不会关:按在一个已经开着的面板上什么都不发生,那读起来是键坏了,而不是面板已经在你要的位置 —— 现在一个键管两头。真机 6/6。

**长录音(F4)** —— 你要的四件事,每一件都翻出一个洞。①第一秒起就有计时,过一分钟还会写明"stops at 10:00" —— 没人应该靠撞上去才知道有上限。②十分钟就是那个上限:麦克风自己停,已经说过的话留着等你采纳(没人结束的录音会一直涨到别的东西先坏)。③音频改成**边说边一秒一存**,而不是结束时一整块 —— 中途意外结束也还剩到最后一秒;顺带修掉一个新引入的死结:上限触发后 `stop()` 还在等一个早就发生过的 `onstop`,十分钟的话会卡在 Accept 上永远回不来。④**转写失败现在说得出录音在哪** —— 音频本来就在模型之前写盘,但调用方只拿到失败,录音躺在盘上够不着,那和丢了一样;现在 capture_id 跟着错误回来,新路由 `POST /v1/captures/{id}/transcribe` 在原音频上重来,浮层给出入口。"什么都没听到"走同一条路:只说"音频留着"却不给任何去处,读起来就是"留着也没用"。另外离线队列改成**按字节**而不是按条数:十条十分钟录音是 25MB,而这个存储只有 10MB —— 按条数算的话,拒绝你的是配额、从别处、关于一个你看不见的东西。真机 7/7(真实语音)。

**页面级 Skill 进右键菜单(F1)** —— 在网页上右键,关于"整页"的 Skill 就在那里;哪些算页面级,读的是 Skill 自己的 `contexts`,不存在第二份要手工同步的清单。点一个,Side Panel 打开,里面两条消息:先说这次跑的是哪个 Skill,再是它的产出。页面在生成之前先被存成 Source —— 答案要站在一个存在、事后能追过去的东西上,这是别处都遵守的规矩,菜单不该是例外。真机 8/8(真实模型)。路上撞出一个坑:消息的类型联合和守它的运行时白名单是同一件事的两半,只加一半照样类型检查通过、到了运行时被丢掉 —— "读这一页"什么都不答就是这个原因。诚实的边界:Chrome 不合成右键点击,那一跳只在真实使用里发生,两边都验过了。

**"Already recording." 的真相(X19)** —— 队列里那个假设(换代后同页两份 Logue 抢录音)**我实测推翻了**:在真实网站上每 0.4 秒采样十五秒,连没有修复的构建也从没出现过两份 —— Chrome 在扩展 reload 时就把旧内容脚本的上下文杀掉了,旧的那份没机会把元素抢回去。真实链条更简单,而且已经修好了:**X10 的死锁**冻住了那个浏览器的自更新,于是长期开着的标签页一直跑着 B14 之前的构建 —— 那句话只存在于那个构建里;而它的修复只能通过它自己修的那条路送达。**证据取自你自己的浏览器**、开了一整天的那个真实 Notion 页面:一个宿主、构建戳是几分钟前、ready、无错误、页面上没有那句话。自更新重新够得着已打开的标签页了。留下的是防御性的一层,并且标明是防御:实例进场时登记一个退场方式,下一个实例挂载前先调用它;顺带修正一个真实的次序错误 —— 原来先移元素、后停观察器,而观察器会把元素塞回去。我自己第一版检查又是"过了什么都没证明":在 reload 之后 10 秒才数宿主,而旧脚本的孤儿轮询正好 10 秒 —— 是**拿掉修复跑同一个检查**才发现的。

**key 泄露的清场(S2)** —— 你的 Gemini key 随一份工作区备份进了公开仓库:`.gitignore` 写的是 `.logue-data/`,匹配不到重建时留下的 `.logue-data.before-rebuild-111251/`,455 个文件被我一次 `git add -A` 不看清单就提交了。Google 扫到,封了 key。清场按你的指令做完:本地历史用 filter-repo 重写(pickaxe 复扫,任何提交里不再有 key,也没有任何 AIza 形状的串);建了全新私有仓库(它从没见过那个对象,`4a420d33` 在上面是 No commit found),推入 main 461 个提交和 15 个标签;删掉旧仓库 —— 悬空提交和 13 个 v0.2.x 旧 release 随它一起消失;新仓库改名顶位 `ralphite/logue`,本地 origin 对齐。防再犯:pre-commit 闸(`scripts/check-secrets.sh`)拦工作区文件、按形状拦 key、超 120 个文件的提交要人点头,拿真 key 试过拦得住。**旧 key 已作废,必须换新的** —— 这是唯一剩下的事。重写前的全量备份在你磁盘上:`~/logue-before-rewrite-20260809-002546.bundle`(内含旧历史和 key,只此一份,别推到任何地方)。

**Host(S1)** —— 只绑回环、校验来源,拿不出 Logue 身份的页面不许写。

**面板里的 Dictation(N4)** —— 你要的五件事:新 tab、页面文字当 context、音频和文字都在、一键复制加 Skill 改写、多个任务并行。<br><br>**界面走了四版才对。** 第一版我提了四个问题,你指出四个**都是我自己造出来的** —— 最典型的是"中文页的上下文怎么取",根源是我擅自把你说的"送页面正文"改成"送生词表",而挑专名靠大写字母。撤掉。第二版你说 ASCII 画的框没法看,改成能点的 HTML;你发来 ChatGPT 听写的截图,**录音控件在原地变形**,而我把 Record 放底部、✓✕ 放顶部 —— 手要在面板上下跑。第三版你把版本编号、语音选项、待转计数、10:00 提示全砍了。**最后是 v4**:整个界面只剩四个动作 —— 录、播、复制、套一个 Skill,**lineage 用缩进说**,不用编号。<br><br>**几个决定**:录音控件**只有一个**,平时是 `Record`,按下之后同一个框变成 `✕ · ● 计时 · 波形 · ✓`,Esc/Enter 和光标旁那条同一套键;**每一段文字长得一样** —— 转写不特殊,它只是第一段,和它生出来的每一版共用同一个组件、同一排"复制 + 还没用过的 Skill";**用过的 Skill 在那一段上就消失**;**没有关闭/删除**。播放器换掉了浏览器原生那个:一个控件、**一个时间**(空闲总长、播放时位置),面板和 Web 应用共用。<br><br>**真机 13/13,真麦克风、真 Host、真 Gemini。** 为此给 QA harness 加了 `LOGUE_TEST_REAL_MIC=1`:Chrome 的文件假麦克风在这台机器上**始终是静音**(peak 0,连立体声 48k 重编码也一样,README 里量过两次),所以改成用真麦克风,`say` 把句子说给它听 —— peak 0.0434,400KB/5 秒。这是唯一能在真浏览器里拿到真转写的路。<br><br>**路上抓到两个真 bug,都是真机跑出来的**:①`readablePageText()` 用"含空格"判断正文,中文句子没有空格 —— 真实中文维基页 440 块丢 362 块,正文段落全丢,而这条抽取器今天就在服务"Save this page";②**改写把待处理的文字当成 `Request:` 送给模型**,模型把这个标签写进了自己的答案(`Request: Test voice input\nWe should…`),第二次改写还会叠加成 `# Request: Request:`。现在 `POST /v1/runs` 把两件事分开:`instruction`(要什么)和 `input`(处理什么);顺带 `origin_id` 让改写不再把转写**再存一份**当"活动记录"。

**vibedoc 与 agentrunner 复核(C1、U3、U4)** —— 读完、列清单、做减法,而不是照单全收。

**不用催也能接着干** —— cron 做不到,因为它只在会话空闲的那一瞬间触发,落在任务中间的每一次都被跳过。会退出的后台命令和 Monitor 心跳都能把会话叫醒,与忙闲无关。这是跑出来验证的,不是假设的。

**"滚到可见"这次真的滚了(P6b)** —— P6 结束时我自己记下的那条"没验到"。原来那段测试文字在 Logue 自己的 Stream 页上**自始至终都在视野里**,所以 locate 之后它还在视野里,证明不了任何事。<br><br>现在换成**真内容、真会滚的页面**([p6b.mjs](../../scripts/qa/p6b.mjs)):Wikipedia 的《Special relativity》,**48240px 高,窗口 757px**。在整页 66% 处选一段存下,回到顶端,**先断言 `scrollY=0` 时它在视野外**(段落在 31539px),再让面板发 `logue:locate`。页面滚到 **31190**,段落停在 **349px** 处、被选中、整段落在窗口内。**12/12。**<br><br>关键是那条**"之前在视野外"的断言** —— 没有它,后面每一条在一个从不需要动的页面上也照样全绿,而那正是 P6 当时的处境。<br><br>路上抓到一个真问题:**https 页面根本 fetch 不到 `http://127.0.0.1` 的 Host**(混合内容),第一次跑把它报成了功能坏掉。断言现在从面板那边发,页面那边只被 `executeScript` 问"它在哪"。

**`/logue` 在活着的会话里真的被触发了(G1b)** —— G1 剩下的最后一步。skill 装好过、49 篇真实文档的往返也验过,唯独"这条命令在一个活的 Claude Code 会话里被调起"没证过 —— 嵌套的 `claude -p` 子进程做不了准,它证明的是那个子进程,不是这条路。<br><br>这一次就在**当前这个会话**里按名字调起 `logue`,harness 把 `~/.claude/skills/logue/SKILL.md` 载进这一轮,再照它说的跑 `logue.py read`,打到真 Host(127.0.0.1:8787),读回 `doc_efbbfbd5c5ce02c8`《Logue QA — V7 rewrite》revision 2 和正文。<br><br>**没证到的那半说出来**:是我在活会话里按名字调的,不是你亲手敲 `/logue`。机制上是同一条路 —— 都是 harness 载入同一份 SKILL.md。

**审计结案,靠复量而不是靠标记(A2)** —— 你的原话是"都给完成一下"。六条当时都标了 ✅,**但标记不是证据** —— 而且从那以后面板多了 Dictation、多了 Storybook、多了 N2–N8 一整轮。所以按第一轮的老办法**重新量了一次**:真浏览器、真工作区、真内容,读计算样式。<br><br>**先修的是走查脚本自己。** [audit-ext.mjs](../../scripts/qa/audit-ext.mjs) 只量"默认打开的那一屏";面板改成默认开 Dictation 之后,它第一行 `document.querySelector('textarea')` 拿到 null,**整个走查死在 `Illegal invocation`** —— 也就是说**新加的那个面从来没被量过**。现在它按标签逐个切、逐个量,并汇总"跨 tab 的字号集合"和"低于 24px 的点击目标"两行。**面板长出一个面,走查就得跟着长。**<br><br>**复量抓到三条漏网的,都修了**:①`DocumentsRoute` 的 `<h2>{n} Sources</h2>` **字重还是 400**,另外四个 `h2` 都已经 560 —— 和当初 Project 里那条是同一个字符串、同一个病,只修了一处;②对话里的引用角标 **`[Source n]` 量到 20×20 / 22×20**,低于 24px 地板,而 R13 之后它是最常被点的东西之一;③Dictation 里"N recordings without words"那**一整行是控件,只有 16px 高**。②③用的都是当初那三处低矮链接的老药方:**内边距做命中区、负外边距还回版面**,角标视觉仍是 20px 药丸(它坐在 13px 正文行里),盒子 24px。<br><br>**改完复量:低于 24px 的一个都没有**(改前 5 个),字号仍只有 12/13 两档,四个 tab 都不横向溢出。四道门禁全绿(typecheck / lint / 前端 92 条 / Host 157 条 + integrations 27 条)。<br><br>**一处没改,要你一句话**:`Menu` 的快捷键字母和菜单分组标题仍是 **11px**,全产品仅剩这两处 —— 它们是 **X33** 特意照 Claude Code 做的、你确认过的,**不是漏网,是和审计 #4 冲突的一个决定**。我没有自作主张推翻它,理由和取舍写在 [audit.md](audit.md) 末节。
