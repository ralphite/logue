# N13 · 面板收成「一个列表 + 一个输入框」

2026-08-13,你的原话在 [cc/tasks.json](../../cc/tasks.json) 的 **N13** 上,一个字没改。

**状态:骨架已拍板 —— A / 留 / 书签(2026-08-13),已实现并部署。**
mock 在 [n13-panel-mock.html](n13-panel-mock.html);真浏览器检查在
[scripts/qa/n13.mjs](../../scripts/qa/n13.mjs)(真文章 + 真 Host,11 条断言)。
下面第六节的三个问题保留原样,因为它们是这次改动的依据。

**追加(2026-08-14):要 mode-picker,加在已上线版本上面。** 第五节的
Comment / Question / Task 三个模式是要做的,**加在** `extension/src/panel/Composer.tsx`
现在这版之上,不是重做。「记进某个文档」**不动** —— 22:12 已经裁决留下,这次只问了
mode-picker,没有说要连文档 chip 一起改;下面第七节那句"删掉"是没被采纳的旧读法,
已划掉。**第八节"要几个模式"还没定** —— 先按 Comment / Question / Task 三个动手,
Task 是我加的、他没确认过,做完让他看一眼再定去留。

---

## 一、我的理解(一句话)

面板只剩两块:**上面是一条列表**,**下面是一个输入框**。
说话和打字都进这一个输入框,**语音只负责把字填进去,不负责发**;发出去的每一条,底下挂 Ask / As Markdown / Into English 这些技能。

## 二、现在长什么样,为什么要砍

今天的面板([sidepanel.tsx](../../extension/src/sidepanel.tsx),1838 行)有 **三个动词按钮**(Record / Keep / Ask)、**两个录音条**(Ask 框里一个、Record 那个 `RecordControl` 一个)、**四段互不相干的列表**(转写、对话、这页存过的、排队中的),还有一个只在录音时才有意义的隐藏状态 `speaking: "chat" | "dictation"` —— 它存在的唯一理由,就是「有两个地方能说话,得记住这次是对谁说的」。

一个输入框之后,这个状态、这两个录音条、这三个按钮里的两个,全部消失。

| 今天 | 之后 |
|---|---|
| Record 按钮(录音 → 直接存成 Source) | 输入框里的麦克风 |
| Ask 按钮(展开第二个输入框) | 没有了,Ask 是每条记录底下的技能 |
| Keep 按钮(存整页 / 存选区) | 整页 = 输入框那行的书签图标;选区 = 引用块进框,直接提交 |
| 两个录音条,行为不同 | 一个录音条 |
| 四段列表 | 一条列表 |

## 三、输入框(composer)

```
┌──────────────────────────────────────────┐
│ ▎FROM THIS PAGE                     ✕    │  ← 选区引用,可删
│ ▎Multi-head attention allows the model…  │
├──────────────────────────────────────────┤
│ 在这里打字,或者点麦克风说                │
│                                          │
├──────────────────────────────────────────┤
│ [💬 Comment ▾]            🔖   🎤   [ ↑ ] │
└──────────────────────────────────────────┘
```

左边那个 chip 是**模式**:这一条是什么,要不要有人回答。它的颜色就是这条记录进列表之后徽章的颜色。
右边三个:🔖 存整页(不用打字,一下点掉)· 🎤 说 · ↑ 提交。

**默认范围是整页。** 你在页面上选中一段,面板的输入框自动出现这段的 **Markdown 引用**(`> …`),范围就变成这段。取消选中或点 ✕,回到整页。

技术上要新加一条消息通道:content script 监听 `selectionchange`,把选区文字**和 anchor**(已有的 `TextAnchor`)推给面板。今天面板是临时用 `chrome.scripting.executeScript` 去拿的,拿不到 anchor,也就没法在原地找回来。带上 anchor,这条评论以后能被定位回页面上的那一段。

**长选区**:引用块超过一定长度(建议 600 字)在框里折起来,存下去的是全文,不截断。

## 四、语音(照 chatgpt.com,参考 agentrunner)

录音时输入框不换形状,只多一条:

```
┌──────────────────────────────────────────┐
│ 已经打进去的字…                          │
├──────────────────────────────────────────┤
│ ● 0:12  ▁▃▇▅▂▇▃▁▂▅▇▃▁▅▂▇▃▁▂▅▇▃▁▂▅▇▃▁▂   │  ← 录音中
├──────────────────────────────────────────┤
│ [💬 Comment ▾]           ✕    ✓    [ ↑ ] │
└──────────────────────────────────────────┘
```

| 控件 | 做什么 | 键 |
|---|---|---|
| ✕ | 扔掉,什么都不留 | `Esc` |
| ✓ | 转成文字**插进输入框**,不发 | `↵` |
| ↑ | 转成文字插进输入框**并且提交** | `⌘↵` |

录音时输入框不变形:上面多一条紫色的 ● 计时 + 波形,下面那一行的右边由 `🔖 🎤 ↑` 换成 `✕ ✓ ↑`。
**提交键始终在同一个位置**,模式 chip 也一直看得见 —— 按 ↑ 之前永远知道这一条会变成什么。

- **插入 ≠ 提交。** 插完可以接着说第二段、第三段,可以打字改,发不发是另一个动作。这正是你要的「允许用语音多次地输入」。
- 插入落在**光标处**,不覆盖已经打的字 —— 这条今天就有(`joinAtCaret`),保留。
- 转写在后台跑,麦克风立刻放开:说完一段马上能说下一段,不用等模型。
- 转写失败不吃录音 —— 录音先落盘再送模型,失败在列表里出一条带 **Try again** 的记录。这是 [behaviors.md](behaviors.md) 里不能碰的一条。

**这里有一条既有行为被你改掉了**,我照改并记档:
今天「⌘⇧K 开面板即录音,`↵` 直接把话发进对话」。之后 `↵` 只是插进输入框,要再按一次 `⌘↵` 或点提交才发。

## 五至八节:2026-08-14 确认要做,加在已上线版本上

以下四节是这份方案的第二轮(2026-08-13 22:14 写的),当时和另一个会话拿到的
22:12 裁决(A/留/书签,没有 mode-picker)撞车了。2026-08-14 你说清楚了:
**mode-picker 要,加在已上线版本上面。** 「记进某个文档」不受影响,继续按
22:12 的裁决留着。下面按这个来改 `extension/src/panel/Composer.tsx`,不是重写。

## 五、提交模式(你说的「几个不同模式」)

提交之前先选这一条是什么。我画了三个:

| 模式 | 什么意思 | 提交之后 |
|---|---|---|
| **Comment** | 记下来。没有人回答。 | 一条记录进列表,自动归档,底下挂技能条 |
| **Question** | 要一个回答,从你自己的材料里找 | 记录 + 缩进在下面的 **Answer**,带 `[Source n]` 可点开 |
| **Task** | 要它去做一件事 | 记录 + 它的提议(存进某 Project / 起草一份文档),**你点头才发生** |

**Task 是我加的**,不是你说的。理由:agent 本来就能提这类提议,今天只是没有入口去要它做。你可以只要前两个。

模式不改变输入框,只改变提交后发生什么 —— 所以说到一半改主意,换个 chip 再提交就行,不用重打。

## 六、列表:一条流,每条带类型

提交产生一条记录,进同一条列表,新的在上。类型靠 24px 徽章 + 一个动词区分 —— 跟 Web 应用 Activities 那边同一套语言(C2),不另发明一套。

| 类型 | 怎么产生 | 底下存成什么 |
|---|---|---|
| **Comment**(打字) | 输入框有字,提交 | `kind: text` |
| **Comment**(口述) | 语音插入后提交 | `kind: voice`,音频可回放 |
| **Quote** | 输入框只有选区引用,提交 | `kind: selection`,带 anchor |
| **Page** | 存整页 | `kind: page` |
| **Answer / Rewrite** | 某条上跑了技能 | `kind: derived`,缩进挂在母记录下 |

带引用提交时,评论的 `parent_ids` 指向那段引用 —— 「对某段话的评论」这个结构库里已经有了,不用新造。

**每条记录底下是技能条**:`Ask` · `As Markdown` · `Into English` · `⋯`。
这正是你说的「submit 的东西下面有一个 ask markdown into English 这种东西」。今天只有口述的转写才有这一条(`DictatedText`),之后打字的、存的选区、存的整页,一视同仁 —— 它们本来就是同一种东西。

技能跑出来的结果**缩进挂在母记录下面**,不是新起一条。今天口述就是这么做的,保留。

排队中/转写中/失败的录音,不再是列表上方一个独立的盒子,而是列表里一条**状态未完成的记录**。

## 七、已经定下的(2026-08-14 更新)

- ~~「记进某个文档」删掉~~ —— **没被采纳。** 22:12 的裁决是留下,已经上线成
  `Composer.tsx` 里 "Into · <文档名>" 那个 chip。这次只加 mode-picker,不动它。
- **「存成页」留下。** 是输入框那一行的书签图标,不用打字,一下点掉。（已上线)

## 八、Task 这个模式还没确认

先按三个模式(Comment / Question / Task)做。**Task 是我加的**,你还没点头过——做完给你看一眼实物,你说去掉就去掉,不影响 Comment / Question 这两个已经确认的。

## 九、怎么落地(每步一个可验证的提交)

| 步 | 做什么 | 验证 |
|---|---|---|
| 1 | 选区通道:content → panel 推 `{text, anchor}` | 真站上选中,面板出现引用 |
| 2 | Composer 组件(输入框 + 模式 chip + 存页 + 麦克风 + 提交),替掉三个动词和第二个输入框 | Storybook 全状态 |
| 3 | 录音条三键(✕ / ✓ / ↑)+ 三个键位 | 真麦克风,连说两段 |
| 4 | 一条列表:转写、对话、存过的、排队中的合成一个记录模型,带类型徽章和技能条 | 真 Host,五种类型各出一条 |
| 5 | 删代码:`speaking`、`asking`、`Thread`、`Kept`、`IntoDestination`、旧的 `RecordControl`、旧的 askBox;Storybook 重拍 | 四道门 + 行数应显著下降 |
| 6 | [behaviors.md](behaviors.md) / [copy.md](copy.md) 更新,真浏览器在 Notion + 一篇真文章上走一遍 | CUJ |

## 十、不能被这次改动弄丢的(已在 behaviors.md 里)

- 录音先存后转写;模型失败给 Try again,不给道歉。
- Host 关着也能录,排队等它回来。
- 麦克风不会卡死;「Already recording.」永远不许出现在屏幕上。
- 一次只有一个 Logue 界面在屏幕上;浮层不压住正在读/写的字。
- 每条记录都能被追回它的出处。
- 面板、注入控件、Web 应用三处数据自动同步,不需要刷新。
